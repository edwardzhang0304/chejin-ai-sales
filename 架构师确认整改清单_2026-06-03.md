# 架构师确认整改清单

日期：2026-06-03

负责人：架构师 / 技术负责人

适用范围：当前 P0 运营后台代码进入 GitHub 前的工程质量确认。

## 1. 结论

当前代码可以作为 P0 运营后台测试版继续测试，但在以下 P0 必改项完成前，不建议推送到 GitHub 正式分支。

若需要临时推送，只允许推送到评审分支，并在提交说明中明确：

```text
P0运营后台阶段代码，待安全、迁移、错误响应和后端测试环境治理后再作为正式工程基线。
```

## 2. P0 必改清单

以下问题阻塞推送 GitHub 正式分支。

| 编号 | 问题 | 责任角色 | 阻塞原因 | 通过标准 |
|---|---|---|---|---|
| P0-01 | 手机号当前为 base64 伪加密 | 后端工程师 | 涉及客户手机号安全，不能作为正式工程基线 | 改为真实加密方案，或明确仅 P0 内测可用并禁止生产使用；配置中不得使用默认密钥 |
| P0-02 | 错误响应缺少统一 `trace_id/request_id` | 后端工程师 | 不符合“错误码可解释、可运维排障”原则 | 所有业务错误、参数错误、未知异常均返回稳定 `code/message/trace_id` |
| P0-03 | 未知异常缺少统一兜底错误码 | 后端工程师 | 可能出现 500 且前端无法定位 | 增加全局异常处理，返回 `INTERNAL_SERVER_ERROR` 或等价稳定错误码，并记录日志 |
| P0-04 | Header 操作人 ID 非法时可能触发系统异常 | 后端工程师 | 坏请求不应变成系统错误 | `X-Operator-Id` 非 UUID 时返回 `OPERATOR_ID_INVALID` 或 `VALIDATION_ERROR` |
| P0-05 | 生产环境仍可能自动建表 | 后端工程师 | 正式工程不能依赖 `AUTO_CREATE_TABLES=true` | 区分 dev/prod；生产禁止自动建表 |
| P0-06 | 缺少 Alembic 数据库迁移体系 | 后端工程师 | 后续字段变更不可追踪、不可回滚 | 创建 Alembic 目录和首版迁移脚本；README 写清迁移命令 |
| P0-07 | 后端测试环境未跑通 `pytest` | 后端工程师 | 无法证明核心接口可回归 | 在标准环境中执行 `pytest` 通过，并输出测试记录 |
| P0-08 | 核心接口测试覆盖不足 | 后端工程师 / 测试工程师 | 重构或修复时容易改坏主链路 | 覆盖新增、重复手机号、分配失败、重新分配、无效/恢复、导出脱敏、手机号明文审计 |
| P0-09 | 前端错误展示未包含可排障信息 | 前端工程师 | 用户截图无法定位问题 | 前端展示错误文案，同时保留 `code` 和 `trace_id` |
| P0-10 | 提交范围未最终确认 | 前后端工程师 / 技术负责人 | 不能把依赖、构建产物、缓存、数据库提交到 GitHub | `node_modules`、`dist`、`__pycache__`、`.db`、`.env` 不进入提交；提交前执行 dry-run 或等价检查 |

## 3. P1 可延后清单

以下问题不阻塞 P0 测试，但不建议拖到后续自动化模块开发之后。

| 编号 | 问题 | 责任角色 | 风险 | 建议完成时机 |
|---|---|---|---|---|
| P1-01 | `lead_service.py` 文件过大 | 后端工程师 | 后续继续加功能会变成大杂烩 | P0 通过后、下一阶段开发前 |
| P1-02 | 前端 `LeadsPage.tsx`、`LogsPage.tsx` 偏大 | 前端工程师 | 页面逻辑、交互、请求混在一起，维护成本上升 | P0 通过后 |
| P1-03 | `global.css` 过大 | 前端工程师 / UI 设计师 | 样式影响范围不清晰，后续容易互相覆盖 | P0 通过后 |
| P1-04 | 详情接口存在局部 N+1 查询风险 | 后端工程师 | 数据量增长后性能下降 | P0 通过后压测前 |
| P1-05 | 前端 API client 默认响应一定是 JSON | 前端工程师 | 遇到网关错误、空响应、HTML 错误页时异常不友好 | P0 通过后 |
| P1-06 | Docker、本地、测试启动文档需统一 | 后端工程师 / 测试工程师 | 新人和测试环境复现成本高 | P0 测试准入前后 |
| P1-07 | 前端浏览器冒烟测试未纳入标准命令 | 前端工程师 / 测试工程师 | 手工验收成本高 | P0 测试阶段 |

## 4. P2 后续项

以下事项属于后续正式产品化或下一阶段自动化能力，不阻塞当前 P0 运营后台测试。

| 编号 | 后续项 | 责任角色 | 说明 |
|---|---|---|---|
| P2-01 | 正式登录与权限体系 | 后端工程师 / 产品负责人 / 架构师 | 当前使用 Header 模拟操作人，后续需接真实鉴权 |
| P2-02 | 生产密钥管理 | 后端工程师 / 运维负责人 | 后续接 KMS 或等价密钥管理能力 |
| P2-03 | Worker 执行台 | Worker 工程师 / 架构师 | 当前 P0 不包含 |
| P2-04 | RPA 加微信 | Worker 工程师 / 后端工程师 | 当前 P0 不包含 |
| P2-05 | AI 自动回复与会话状态机 | 后端工程师 / AI 工程师 / 架构师 | 当前 P0 不包含 |
| P2-06 | 图片理解与图文回复 | AI 工程师 / 后端工程师 | 当前 P0 不包含 |
| P2-07 | 自动召回 | 后端工程师 / 产品负责人 | 当前 P0 不包含 |
| P2-08 | 飞书接管通知 | 后端工程师 | 当前 P0 不包含 |
| P2-09 | 多商户 SaaS 化 | 架构师 / 后端工程师 / 前端工程师 | 当前一期不做 |

## 5. 推 GitHub 判定标准

### 5.1 不允许推正式分支

出现以下任一情况，不允许推送到 GitHub 正式分支：

- P0 必改项未完成。
- 后端 `pytest` 未在标准环境跑通。
- 手机号仍使用 base64 伪加密且未明确禁止生产。
- 错误响应没有 `trace_id/request_id`。
- 生产环境仍依赖自动建表。
- 缺少数据库迁移脚本。
- 提交范围可能包含依赖、缓存、数据库或密钥文件。

### 5.2 允许推评审分支

满足以下条件时，可以推送到评审分支：

- 当前 P0 功能可运行。
- 前端构建和测试通过。
- 后端语法检查通过。
- 已知问题在提交说明中明确列出。
- 分支名称明确标识为评审或待整改，例如：

```text
p0-ops-admin-review-needed
```

### 5.3 允许推正式工程基线

满足以下条件时，允许推送正式工程基线：

- P0 必改项全部完成。
- P0 接口测试通过。
- P0 UAT 通过。
- 前端构建、前端测试、后端测试全部通过。
- 错误码、trace_id、审计日志、手机号脱敏和加密均符合方案。
- 提交范围干净。

## 6. 架构师确认意见

当前阶段建议：

```text
先完成 P0-01 至 P0-10；
再由架构师做第二轮代码审核；
审核通过后再推 GitHub 正式分支。
```

P1 项可登记为工程治理任务，不阻塞 P0 测试，但不应拖到 Worker / AI 自动化模块开发之后。
