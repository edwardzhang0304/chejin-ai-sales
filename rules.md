# 项目 Rules 总入口

版本：v0.4

日期：2026-06-03

用途：本文件只做规则索引和全局口径，不再承载所有角色细则。具体执行规则按角色拆分到 `rules/` 目录，避免多人会话继续把项目经理、产品、UI、前端、后端、测试规则混写在一个文件里。

## 1. 使用顺序

每次输出项目结论、分工、验收判断、测试判断或开发安排前，必须先按角色读取对应规则：

| 场景 | 必读规则 |
|---|---|
| 项目进度、责任分工、里程碑、UAT、风险升级 | `rules/project-manager.rules.md` |
| PRD、范围、不做范围、设计稿、UI 差异、验收口径 | `rules/product-ui.rules.md` |
| 前端实现、Vite、页面、弹窗、交互、浏览器验收 | `rules/frontend.rules.md` |
| 后端接口、Docker、健康检查、pytest、镜像、接口契约 | `rules/backend.rules.md` |
| 测试准入、测试报告、UAT、回归、缺陷、自动化 | `rules/testing.rules.md` |
| 文档总目录、变更日志、日报、交付物、合同边界 | `rules/delivery-docs.rules.md` |

如果一个问题跨角色，例如“前端 UI 是否可验收”，必须同时看 `product-ui`、`frontend` 和 `testing` 三份规则。

## 2. 全局硬规则

1. 不允许基于记忆、印象或单一文档判断项目状态。
2. 不确定的信息必须标注【待确认】，不得用推测补齐。
3. 任何“已完成、未完成、是否可测、是否可验收、是否可上线”的结论，必须能追溯到文件、测试报告、代码、日志或用户明确口径。
4. 发现前次结论遗漏或错误，必须先纠正事实，再继续推进。
5. 功能通过不等于 UI 通过；接口通过不等于前端通过；测试通过不等于客户验收通过。
6. 不允许把“v2.6 完整技术蓝图”当作“当前 P0 全部交付范围”。
7. 不允许把“PRD v0.1 当前范围”理解为“技术蓝图删减”。

## 3. 当前项目固定口径

1. 本期先做人工录入/管理线索，不做抖音 API 自动接入。
2. 抖音开发者开放平台已审核通过，但 API 接入放到下一期。
3. 不再生成对外资料包，后续客户可见内容只看正式交付物。
4. PRD v0.1 只圈定当前阶段业务范围，不影响技术方案 v2.6 完整蓝图。
5. v2.6 是完整技术蓝图，不等同于当前 P0 全部交付范围。
6. 线索导入模板直接采用产品经理和 UI 设计师的结果。
7. 当前 UI 验收基准以本地设计稿/本地静态稿为准；Figma 因额度问题暂不能作为当前最终上传和同步基准。
8. 当前 P0 主要验收桌面运营后台；移动端 390px 横向溢出如不纳入 P0，可列 P1 遗留。
9. 当前 Vite 前端已有 9/9 冒烟通过证据，但新增客户弹窗与本地设计稿不一致，不能按 UI 通过处理。
10. 后端 BUG-001 已回归通过，但正式 Docker 镜像重建、容器内 pytest 和交付环境验证仍需单独确认。
11. 新增客户弹窗中，联系方式固定为手机、微信、邮箱三个字段；手机必填，微信和邮箱可选，不提供添加/删除联系方式。
12. 新增客户弹窗中，自定义信息支持添加字段和删除字段；自定义字段由“字段名称 + 字段内容”组成，不固定为意向车型、预算、城市。

## 4. 最终答复前自检

发送最终答复前必须自问：

1. 是否查了对应角色规则？
2. 是否查了产品、UI、前端、后端、测试、交付/合同中与本问题相关的证据？
3. 是否把【待确认】写成了确定结论？
4. 是否存在旧报告被新报告覆盖但没有说明？
5. 是否需要同步更新 `项目文档总目录.md`、`文档变更日志.md`、日报或责任分工？
