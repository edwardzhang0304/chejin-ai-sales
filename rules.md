# 项目 Rules 总入口

版本：v0.8.3

日期：2026-07-20

用途：本文件只做规则索引和全局口径。具体执行规则按角色拆分到 `rules/` 目录，避免多人会话继续把项目经理、产品、UI、前端、后端、测试规则混写在一个文件里。

## 1. 使用顺序

每次输出项目结论、分工、验收判断、测试判断或开发安排前，必须先按角色读取对应规则：

| 场景 | 必读规则 |
|---|---|
| 项目进度、责任分工、里程碑、UAT、风险升级 | `rules/project-manager.rules.md` |
| PRD、范围、不做范围、设计稿、UI 差异、验收口径 | `rules/product-ui.rules.md` |
| 前端实现、Vite、页面、弹窗、交互、浏览器验收 | `rules/frontend.rules.md` |
| 后端接口、Docker、健康检查、pytest、镜像、接口契约 | `rules/backend.rules.md` |
| 测试准入、测试报告、UAT、回归、缺陷、自动化 | `rules/testing.rules.md` |
| 文档总目录、变更日志、日报、交付物、合同边界 | `rules/delivery-docs.rules.md` |

如果一个问题跨角色，例如“销售管理是否可进入测试”，必须同时看 `product-ui`、`frontend`、`backend` 和 `testing` 四份规则。

## 2. 全局硬规则

1. 不允许基于记忆、印象或单一文档判断项目状态。
2. 不确定的信息必须标注【待确认】，不得用推测补齐。
3. 任何“已完成、未完成、是否可测、是否可验收、是否可上线”的结论，必须能追溯到文件、测试报告、代码、日志或用户明确口径。
4. 发现前次结论遗漏或错误，必须先纠正事实，再继续推进。
5. 功能通过不等于 UI 通过；接口通过不等于前端通过；测试通过不等于客户验收通过。
6. 不允许把“技术方案 v0.8 完整蓝图”当作当前 checkpoint 全部交付范围；C2 V16.104 已包含文字/语音统一顺序和语音锚点修复，但不包含图片识别或 C3/C4。
7. 不允许把 P0、旧阶段文档或低于v0.8的技术方案作为当前 C2 通过依据。
8. 除非用户明确要求追溯历史、清理旧版或比对旧版，所有角色不得打开、引用或依据旧版/废弃文档判断当前需求、设计、开发、测试、验收或交付状态。
9. 项目经理角色不得替架构师定义技术方案、任务类型、接口、状态机、错误码或模块边界；涉及技术归属和架构边界时，必须以架构师已确认文档或用户明确转述为准，否则标注【待架构师确认】。

## 2.1 版本管理硬规则

1. 文档变更前必须先判断变更类型：新增需求、模块级变化、细节级变化、纯修订。
2. 新增需求必须升版本号，并同步更新文档总目录、变更日志和一致性检查。
3. 模块级变化升大版本，例如 `v2.0 -> v3.0`。
4. 细节级变化升小版本，例如 `v2.0 -> v2.1`、`v2.1 -> v2.2`。
5. 纯修订不更改版本号，只更新文档内修订记录和 `文档变更日志.md`；纯修订包括错别字、表述更准确、引用路径修正、已定口径的补充说明。
6. 不能把“新增需求”写成“内部修订”来规避升版；若不确定属于新增还是修订，必须标注【待确认】并先让架构师/产品负责人确认。
7. 新增专项文档必须从自身 `v0.1` 或对应起始版本开始命名，文件名必须包含版本号和日期。

## 3. 当前项目固定口径

1. P0 桌面运营后台已收口，相关旧文档已从活跃目录清理，不作为当前依据。
2. 当前活跃范围按技术方案 C0/C1/C2 推进：C0 线索入库与分配，C1 add_friend，C2 会话绑定/微信监听。
3. PRD v0.4.5 是当前产品范围依据；Worker 客户端正式工程口径为 Windows 单应用 + 内置 OmniAuto RPA 执行组件。
4. 技术方案 v0.8 是当前唯一架构事实源，UML/PUML为派生图示；C2 V16.104
   文字/语音、安全准入、V3授权、统一顺序和语音锚点已完成Windows实机回归。
   图片和C2-C3串行候选代码已接入当前分支，现只允许按技术手册第8.5至8.8节
   删除PID硬门禁、补合同/来源记录并执行Windows UAT，不得重新讨论图片流程。
5. 当前 UI 验收基准以本地静态稿 `website/ops-admin.html/css/js` 为准；Figma 因额度问题暂不能作为当前最终同步基准。
6. 当前版本不验收Mac Worker、飞书通知、C4自动召回、抖音API和批量导入；
   现有C2-C3图片、Brain和发送链只作为本轮完整闭环回归，不新增第二套AI流程；
   add_friend主链路继续以Windows Worker内置RPA自动执行为正式方向。
7. 销售管理升级已包含：Worker 绑定状态、阻塞任务、详情抽屉原地编辑、新增销售可选 Worker、销售详情可绑定/更换/清空 Worker。
8. Worker 管理已包含：Worker 列表、详情、Token、启停、心跳状态、当前任务、绑定销售、重置绑定。
9. 任务中心已完成既定复测并可按模块收口；V16.104文字/语音已完成Windows实机
   回归，当前图片、豆包Vision、Brain和发送完整闭环仍待Windows UAT，不能宣称
   图片或C3真实联调已通过。
10. 抖音开发者开放平台已审核通过，但 API 接入放到下一期。
11. 不再生成对外资料包，后续客户可见内容只看正式交付物。

## 4. 最终答复前自检

发送最终答复前必须自问：

1. 是否查了对应角色规则？
2. 是否查了产品、UI、前端、后端、测试、交付/合同中与本问题相关的证据？
3. 是否把【待确认】写成了确定结论？
4. 是否存在旧报告被新报告覆盖但没有说明？
5. 是否需要同步更新 `项目文档总目录.md`、`文档变更日志.md`、日报或责任分工？
