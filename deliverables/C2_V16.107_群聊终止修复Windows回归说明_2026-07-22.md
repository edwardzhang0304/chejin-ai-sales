# C2 V16.107 群聊终止修复 Windows 回归通过报告

## 交付物

- 安装包：`chejin-worker-client-20260722-v16.107.zip`
- 客户端提交：`b298289`
- SHA256：`c134289cb257420b415d2fa55b4ae10037c12f7ea607079898daa3684fc88a63`
- 当前 API：`http://192.168.2.106:8000/api`
- 稳定回滚包：`chejin-worker-client-20260721-v16.104.zip`

## 最终结论

2026-07-22，测试工程师与用户已完成 V16.107 Windows 真实微信联合回归，结果通过。

- V16.107 由“回归候选”转为当前 C2 正式通过版本。
- V16.105、V16.106 只保留为问题定位历史，不再继续测试或交付。
- V16.104 继续保留为稳定回滚包。
- 本结论覆盖本报告列出的 C2 单聊准入、群聊阻断、文字/语音基础回归、V3 顺序、去重和停止监听边界。
- Vision 图片识别仍未启用，不属于本次通过范围。

## 本次修复

V16.106 实机日志证明，客户端已正确识别右侧标题 `CJTEST01 (6)` 为群聊，但 `open_chat` 漏掉了群聊的立即终止分支，继续点击左侧会话并进入短码搜索。

V16.107 改为：

1. 现有右侧标题 OCR 确认“目标短码 + group”后，在 OmniAuto `open_chat` 内立即终止。
2. “目标短码 + unknown”同样立即终止，不默认为 private。
3. 终止证据直接传给外层，返回 `C2_GROUP_CHAT_NOT_ALLOWED` 或 `C2_CONVERSATION_TYPE_UNKNOWN`。
4. Worker 将上述错误作为定位终止态，不再点击左侧、不再打开搜索框。
5. 当前右侧不是目标短码时，仍保留“首屏候选 -> 短码搜索”正常定位能力。
6. 未新增截图、OCR、坐标、点击、HTTP 接口或数据库迁移。

## Windows 启动命令

以下命令可整段复制到 PowerShell：

```powershell
cd $env:USERPROFILE\Desktop
Expand-Archive -Force .\chejin-worker-client-20260722-v16.107.zip .\chejin-worker-client-20260722-v16.107
cd .\chejin-worker-client-20260722-v16.107\worker-client
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
$env:CHEJIN_API_BASE_URL="http://192.168.2.106:8000/api"
$env:CHEJIN_RPA_MODE="real"
$env:CHEJIN_C2_VOICE_TRANSCRIBE_MAX_DURATION_SECONDS="240"
.\.venv\Scripts\python.exe -m chejin_worker_client.main
```

## Windows 实机结果

1. 客户端显示 `V16.107`：通过。
2. 半角人数后缀群聊 `CJTEST01 (6)` 立即阻断，不再点击左侧，不打开搜索框：通过。
3. 全角人数后缀群聊 `CJTEST01（6）` 使用相同规则阻断：通过。
4. 群聊阻断后不进入消息读取、语音转写、消息入库或回复状态机：通过。
5. 当前停在其他会话且目标 private 单聊位于首屏时，点击唯一目标并进入正确会话：通过。
6. 目标 private 单聊不在首屏时，继续使用短码搜索定位，不受群聊阻断误伤：通过。
7. private 单聊文字、语音、V3 顺序、去重和停止监听基础回归：通过。

## 自动化验证

- 源码及 ZIP 解压包 Worker：`251 passed，29 skipped`。
- OmniAuto compatibility：`204/204`。
- 交互证据：`5/5`。
- 输入节奏：`6/6`。
- 窗口规划：`28/28`。
- mock E2E：通过。
- ZIP：422 files，3,128,520 bytes，`forbidden_entries=[]`。

## 交付判定

- C2：通过，可结束本轮 OmniAuto 同步与 V16.107 修复工作。
- 当前交付包：`chejin-worker-client-20260722-v16.107.zip`。
- 回滚包：`chejin-worker-client-20260721-v16.104.zip`。
- 下一阶段如接入 Vision 图片识别或继续 C3，应另立开发与回归范围，不修改本报告的 C2 结论。
