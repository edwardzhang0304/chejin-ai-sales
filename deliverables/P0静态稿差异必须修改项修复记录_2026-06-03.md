# P0 静态稿差异必须修改项修复记录

日期：2026-06-03

关联文件：`deliverables/P0静态稿差异确认表_2026-06-03.md`

## 1. 结论

确认表中的 5 个“必须修改”项已完成前端修复，并已通过构建、单测、浏览器冒烟、依赖审计和 1440x1000 exact 盒模型复核。

本轮修复对象为 Vite 真实前端 `frontend/`，不是 `website/` 静态页。

补充口径：本轮已按 `website/ops-admin.html`、`website/ops-admin.css`、`website/ops-admin.js` 静态稿基准回退工作区尺寸，不再采用此前将列表/详情拉高到 860px 的方案。

新增客户弹窗口径：新增客户弹窗不按当前 `website` 里的“添加联系方式 + 类型下拉”版本。最终稿为 `deliverables/design_preview/create-lead-modal-final.html`，无右上角 `X`，联系方式固定为 `手机 * / 微信 / 邮箱`，不再有添加/删除联系方式；自定义信息支持 `添加字段`，每行是 `字段名称 + 字段内容 + 删除`。

## 2. 修复明细

| 编号 | 问题 | 修复结果 |
|---|---|---|
| UI-005 | 分页缺页码组 | 已补总数、20条/页、上一页、页码组、省略号、末页、下一页 |
| UI-006 | 新增客户弹窗结构不完整 | 已按最终稿补固定手机/微信/邮箱、动态自定义信息、备注、取消/保存/保存并继续新增、重复手机号顶部强提示、查看原线索入口；已移除右上角 `X` 和联系方式添加/删除 |
| UI-007 | 标记无效弹窗多“线索状态”，且“重复/误录”文案不一致 | 已去掉“线索状态”眉标，原因文案改为“重复误录” |
| UI-008 | 恢复有效没有确认弹窗 | 已新增“恢复为有效线索”确认弹窗，确认后才调用恢复接口 |
| UI-011 | 停用销售仍显示“参与轮询” | 已调整展示规则：`enabled=false` 时固定显示“不参与轮询”，checkbox 禁用且不勾选，并补“历史线索保留”说明 |

## 3. 验证结果

| 项目 | 结果 |
|---|---|
| 后端 `/readyz` | 通过，`{"status":"ok","database":"ok"}` |
| `npm run build` | 通过 |
| `npm run test` | 通过 |
| Vite 浏览器冒烟 | 9/9 通过 |
| `npm audit --audit-level=moderate` | 通过，0 vulnerabilities |
| 1440x1000 exact 盒模型复核 | 列表面板 `x=272 y=240 w=768 h=740`，详情抽屉 `x=1056 y=240 w=360 h=740`，表格区 `x=293 y=461 w=728 h=430`，分页行 `x=293 y=891 w=726 h=62` |
| 新增客户最终稿专项检查 | 无右上角 `X`；联系方式区域无 `添加手机号/添加微信/添加邮箱/删除`；固定 `客户名称 * / 手机 * / 微信 / 邮箱 / 备注内容`；自定义信息支持 `添加字段`，每行包含 `字段名称 / 字段内容 / 删除`；底部按钮为 `取消 / 保存 / 保存并继续新增`；2026-06-03 专项 DOM 检查通过 |
| 新增客户紧凑版尺寸复核 | 弹窗宽 `820px`，输入框高 `40px`，联系方式为三列，备注高 `78px`，底部按钮区高 `64px`，无右上角 `X` |

说明：浏览器冒烟结果中 `browser_error_count=1`，来自重复手机号 `409` 业务错误路径记录，不影响 9/9 通过结论。

## 4. 变更文件

- `frontend/src/features/leads/LeadsPage.tsx`
- `frontend/src/features/leads/components/CreateLeadModal.tsx`
- `frontend/src/features/leads/components/InvalidLeadModal.tsx`
- `frontend/src/features/leads/components/RestoreLeadModal.tsx`
- `frontend/src/features/sales/SalesPage.tsx`
- `frontend/src/styles/global.css`
- `deliverables/test_runs/run_vite_frontend_smoke.js`
- `deliverables/test_runs/visual_review_exact/visual_exact_metrics.json`
- `deliverables/test_runs/visual_review_exact/vite_leads_1440x1000.png`

## 5. 测试建议

建议测试负责人基于 Vite 真实前端重新执行：

1. 分页展示和跳页。
2. 列表面板、详情抽屉和分页行是否回到静态稿 exact 尺寸，不允许再拉高到 860px。
3. 新增客户固定手机/微信/邮箱、动态自定义信息、备注、取消/保存/保存并继续新增；确认无右上角 `X`、联系方式区域无添加/删除。
4. 重复手机号 `409` 提示和查看原线索入口。
5. 标记无效原因文案。
6. 恢复有效确认弹窗。
7. 停用销售不参与轮询展示。

## 6. 当前结论

前端认为 P0 静态稿差异确认表中的“必须修改”项已收口，可进入测试复核。
