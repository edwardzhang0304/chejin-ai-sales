# C2 完整回归测试报告

测试时间：2026-07-05 08:24:31 - 08:34:27 CST  
测试对象：C2 Windows 实机 Worker 会话扫描、目标读取、文本入库、语音真实转写、停止后不尾随、scan-result 稳定性  
Worker：77b5e5b0-c796-4e52-adc7-c17b87816941  
目标短码：CJR8S5K3  
绑定：f4df23de-6a4e-44c3-b641-8b6c6f9cc0c1  
会话：e31966a3-def4-443d-a339-c601b8f15656

## 1. 测试结论

结论：不通过。

本轮 C2 有明显进展：同短码多绑定问题未复现，目标读取链路可以跑通，文本消息已成功入库，`scan-result` 未复现 500，停止后也未尾随定向读取。

但 C2 全流程仍不能通过，核心阻断是语音真实转写未入库：会话列表已出现 `[语音] 1"`，Worker 多次进入 `voice_transcribe_current_chat`，并且后续 `/wechat/messages/ingest` 返回 200，但 `message_events` 中仍没有任何 `message_type=voice`，也没有真实转写文本或 `voice_transcription` 证据。

## 2. 测试范围

| 范围 | 是否执行 | 结果 |
|---|---:|---|
| Worker 在线、锁状态基线 | 是 | 通过 |
| CJR8S5K3 绑定唯一性 | 是 | 通过 |
| read-targets 返回目标 | 是 | 通过 |
| 目标定位/二次确认 | 是 | 通过 |
| 文本消息读取入库 | 是 | 通过 |
| 语音消息真实转写入库 | 是 | 不通过 |
| scan-result 不再 500 | 是 | 通过 |
| 停止后 read-targets 为空 | 是 | 通过 |
| 停止后不再 target_chat/voice_transcribe 尾随 | 是 | 通过 |
| C3 AI 自动回复 | 否 | 不属于 C2 |

## 3. 执行情况

| 北京时间 | UTC 时间 | 事件 | 结果 |
|---|---|---|---|
| 08:24:31 | 00:24:31 | 基线检查 | API/DB 健康，Worker 在线，`read-targets=[]` |
| 08:24:31 | 00:24:31 | 查询 CJR8S5K3 绑定 | 仅 1 条有效绑定，绑定 `f4df23de...`，会话 `e31966a3...` |
| 08:24:46 | 00:24:46 | 启动 C2 读取 | 会话置为 `sales_replied_waiting_user`，绑定置为 `bound/listening/allow_listening=true` |
| 08:24:54 | 00:24:54 | read-targets 返回目标 | 返回 CJR8S5K3，display_name=`CJR8S5K3虾丸` |
| 08:25:07 | 00:25:07 | Worker 进入 `target_chat_locating` | 开始目标定位 |
| 08:25:58 | 00:25:58 | Worker 进入 `visible_hit_message_read` | 开始读取可见消息 |
| 08:26:22 | 00:26:22 | Worker 进入 `target_chat_reconfirming` | 执行目标会话复核 |
| 08:27:08 | 00:27:08 | `/wechat/messages/ingest` | API 200，新增 2 条文本消息 |
| 08:28:31 | 00:28:31 | Worker 进入 `voice_transcribe_current_chat` | 开始语音转写流程 |
| 08:28:55 | 00:28:55 | Worker 再次 `voice_transcribe_current_chat` | 继续语音转写流程 |
| 08:29:36 | 00:29:36 | `/wechat/messages/ingest` | API 200，但无新增 voice |
| 08:30:00 | 00:30:00 | scan-result 快照 | CJR8S5K3 预览为 `[语音] 1"` |
| 08:31:07 | 00:31:07 | Worker 再次 `voice_transcribe_current_chat` | 仍未形成 voice 入库 |
| 08:32:03 | 00:32:03 | `/wechat/messages/ingest` | API 200，但 DB 仍只有 2 条文本、0 条 voice |
| 08:32:35 | 00:32:35 | 停止 CJR8S5K3 | 绑定置为 `disabled/paused/allow_listening=false`，会话置为 `ai_active` |
| 08:32:58 | 00:32:58 | 停后复核 | `read-targets=[]`，Worker 锁释放 |
| 08:33:48 | 00:33:48 | 停后观察 | 停后新增消息 0，仅普通 `first_screen_session_scan` |
| 08:34:23 | 00:34:23 | 最终 Worker 状态 | `locked=false`，无目标读取锁 |

## 4. 测试用例执行表

| 用例编号 | 模块 | 测试场景 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 结论 | 优先级 |
|---|---|---|---|---|---|---|---|---|---|
| C2-REG-001 | 环境基线 | Worker 在线无锁 | API/DB 启动 | 查询 `/readyz`、Worker 状态、`read-targets` | Worker 77b5... | API/DB 健康，Worker 在线，初始无目标 | API/DB 健康，Worker 在线，初始 `read-targets=[]` | 通过 | P0 |
| C2-REG-002 | 绑定唯一性 | CJR8S5K3 无多绑定污染 | 测试前查询 DB | 查询 `wechat_session_bindings where remark_code='CJR8S5K3'` | CJR8S5K3 | 只有 1 条有效绑定 | 仅 1 条有效绑定 `f4df23de...` | 通过 | P0 |
| C2-REG-003 | 目标下发 | read-targets 返回 CJR8S5K3 | 会话状态为等待客户回复，绑定 listening | 调用 read-targets | CJR8S5K3 | 返回目标及正确会话 | 返回会话 `e31966a3...`，display_name=`CJR8S5K3虾丸` | 通过 | P0 |
| C2-REG-004 | 目标读取 | Worker 定位并复核目标会话 | read-targets 返回目标 | 观察 Worker 心跳 | CJR8S5K3 | 进入定位、读取、二次确认 | 进入 `target_chat_locating`、`visible_hit_message_read`、`target_chat_reconfirming` | 通过 | P0 |
| C2-REG-005 | 文本入库 | 客户文本消息入库 | 目标读取中 | 查询 `message_events` | `没有哦`、`回家吃` | 入库为 customer text | 08:27:08 新增 2 条 customer text | 通过 | P0 |
| C2-REG-006 | 语音转文字 | 语音消息真实转写并入库 | 会话出现语音消息 | 观察 `voice_transcribe_current_chat`，查询 `message_events` | `[语音] 1"` | 新增 `message_type=voice`，`content` 为真实转写，`voice_transcription` 有证据 | 多次进入语音转写，`messages/ingest` 200，但新增 voice=0 | 不通过 | P0 |
| C2-REG-007 | scan-result 稳定性 | scan-result 不再 500 | Worker 周期扫描 | 查看 API 日志 | scan-result | 全部 200，无 FK 错误 | 08:25:00、08:27:39、08:30:00、08:32:31、08:33:25 均 200，未见 `ForeignKeyViolation` | 通过 | P0 |
| C2-REG-008 | 停止监听 | 停止后不尾随定向读取 | 目标已停用 | 停用绑定；查询 read-targets、心跳、消息 | CJR8S5K3 | read-targets 空，无 target_chat/voice_transcribe，锁释放 | read-targets 空；停后新增消息 0；最终锁释放 | 通过 | P0 |

## 5. 缺陷统计

| 严重等级 | 数量 | 说明 |
|---|---:|---|
| P0 | 1 | 语音真实转写未入库 |
| P1 | 0 | 无 |
| P2 | 0 | 无 |

## 6. 高风险缺陷

### BUG-C2-20260705-01：语音消息被发现并触发转写流程，但没有入库为 voice 消息

严重等级：P0  
影响范围：C2 语音消息收集、后续意图识别、C3 回复准入、线上会话闭环  

复现步骤：
1. 启用 `CJR8S5K3` 监听，确保 `read-targets` 返回会话 `e31966a3-def4-443d-a339-c601b8f15656`。
2. 等待 Worker 进入目标读取。
3. 在目标会话中出现语音消息，scan-result 预览显示 `[语音] 1"`。
4. 观察 Worker 心跳，确认进入 `voice_transcribe_current_chat`。
5. 查询 `message_events` 中本轮新增消息。

实际结果：
- Worker 在 08:28:31、08:28:55、08:31:07 多次进入 `voice_transcribe_current_chat`。
- `/wechat/messages/ingest` 在 08:29:36、08:32:03 返回 200。
- `message_events` 仅有 2 条文本：`没有哦`、`回家吃`。
- 新增 `message_type=voice` 数量为 0。
- 未见 `voice_transcription.raw` 或真实转写文本。

预期结果：
- 语音消息应入库为 `message_type=voice`。
- `content` 应为真实可读的微信转写文本。
- `raw_payload.voice_transcription` 应保留转写证据。
- 若转写失败，应记录失败原因，不应静默 200 且无 voice 事件。

## 7. 性能测试结果

本轮不是压力/性能测试，仅记录接口稳定性：

| 指标 | 结果 |
|---|---|
| `/wechat/sessions/scan-result` | 本轮全部 200 |
| `/wechat/messages/ingest` | 本轮全部 200 |
| API 500 | 未发现 |
| ForeignKeyViolation | 未发现 |
| Worker 锁释放 | 最终释放，`locked=false` |

## 8. UAT 验收结果

不通过。

真实业务视角下，客户文本可以被收集，但客户/会话中的语音消息没有形成可读文本和消息事件。C2 的验收目标不是“Worker 尝试转写”，而是“真实语音内容被正确收集、识别并上报”，本轮未满足。

## 9. 遗留问题

| 问题 | 等级 | 当前状态 | 上线影响 |
|---|---:|---|---|
| 语音真实转写未入库 | P0 | 未解决 | C2 不可完整验收，不能进入 C3 |
| 同短码多绑定/会话漂移 | P0 | 本轮未复现 | 继续纳入回归 |
| scan-result 500 | P0 | 本轮未复现 | 继续纳入回归 |
| 停止后尾随定向定位 | P0 | 本轮通过 | 继续纳入回归 |

## 10. 上线建议

不建议进入 C3。

修复建议：
1. 排查 `voice_transcribe_current_chat` 后为什么 `/messages/ingest` 200 但未插入 voice。
2. 对语音转写失败场景增加可观测错误：例如转写失败原因、空转写、去重过滤、未识别气泡等。
3. 下一轮回归必须重新验证：文本入库、语音真实转写入库、scan-result 200、停止后不尾随、Worker 最终释放锁。

## 11. 当前现场状态

已停干净。

- CJR8S5K3：`disabled/paused/allow_listening=false`
- 会话状态：`ai_active`
- `read-targets=[]`
- 停止后新增消息：0
- Worker 最终：`locked=false`
