# C2 三项阻断回归结果

## 1. 测试结论

**结论：不通过。**

本轮按三项阻断点回归：

1. C2 语音真实转写：**未完成验证**，本轮无新增可验收语音消息入库，不能判通过。
2. 停止后不再尾随定位：**不通过**，停止后仍出现 `voice_transcribe_current_chat` / `target_chat_reconfirming`。
3. `scan-result` 不再 500：**不通过**，本轮仍出现 `ForeignKeyViolation` 导致 `scan-result` 500。

## 2. 测试环境

| 项 | 值 |
|---|---|
| 测试时间 | 2026-07-04 10:38:51 - 10:45:24 CST |
| Worker ID | `77b5e5b0-c796-4e52-adc7-c17b87816941` |
| Client Instance ID | `client_0d961d59-75f1-4d0e-81ea-8677ae9a91a8` |
| 测试短码 | `CJR8S5K3` |
| conversation_id | `29c2d615-7469-484f-b8ce-173bba452541` |
| binding_id | `4ce249c4-a8ba-4519-9558-0f9e2fcc7e64` |

## 3. 时间线

| 时间 CST | 时间 UTC | 节点 | 结果 |
|---|---|---|---|
| 10:38:51 | 02:38:51.548883 | 开启监听 | 成功 |
| 10:39:00 | 02:39:00.558463 | `target_visible_sessions_checking` | 进入目标可见性检查 |
| 10:39:19 - 10:40:17 | 02:39:19 - 02:40:17 | `target_chat_locating` | 持续定位 |
| 10:40:40 | 02:40:40.823225 | `state_target_message_read` | 进入消息读取 |
| 10:41:02 / 10:41:18 | 02:41:02 / 02:41:18 | `voice_transcribe_current_chat` | 进入语音转写步骤 |
| 10:42:04 | 02:42:04 | `/wechat/messages/ingest` 200 | 但 DB 无新增 message_events |
| 10:43:55 | 02:43:55.340333 | 停止监听 | 成功提交停止 |
| 10:44:09 | 02:44:09.717208 | 停止后仍 `voice_transcribe_current_chat` | 不通过 |
| 10:44:31 | 02:44:31.246391 | 停止后仍 `target_chat_reconfirming` | 不通过 |
| 10:44:57 | 02:44:57 | `/wechat/sessions/scan-result` 500 | 不通过 |
| 10:45:04 | 02:45:04.446390 | Worker 锁释放 | 最终释放 |

## 4. 用例执行表

| 用例编号 | 测试场景 | 前置条件 | 测试步骤 | 预期结果 | 实际结果 | 优先级 | 结论 |
|---|---|---|---|---|---|---|---|
| C2-BLOCK-001 | C2 语音真实转写 | 目标会话可读取 | 开启监听，等待 Worker 进入读取和转写，查询本轮新增 voice | 新增 `message_type=voice`，`content` 为真实可读转写文本，raw 有转写证据 | 本轮无新增 `message_events`，无法验证真实转写 | P0 | 未完成验证 |
| C2-BLOCK-002 | 停止后不再尾随定位 | 已开启监听并进入读取链路 | 10:43:55 停止监听，继续查心跳和消息入库 | 停止后 `read-targets=[]`，不再出现该会话的 `message_ingest` / `target_chat_*` / `voice_transcribe_*`，无新增消息 | `read-targets=[]`、无新增消息，但停止后仍出现 `voice_transcribe_current_chat` 和 `target_chat_reconfirming` | P0 | 不通过 |
| C2-BLOCK-003 | scan-result 不再 500 | Worker 周期性首屏扫描 | 查看 API 日志 | `scan-result` 全程 200，无外键错误 | 10:44:57 出现 `ForeignKeyViolation`，`scan-result` 500 | P0 | 不通过 |

## 5. 关键证据

### 5.1 本轮无新增消息

```text
new_messages=0
new_voice_messages=0
latest_ingested=null
```

### 5.2 停止后尾随操作

```text
2026-07-04 02:44:09.717208+00 | voice_transcribe_current_chat | operation_type=message_ingest
2026-07-04 02:44:31.246391+00 | target_chat_reconfirming      | operation_type=message_ingest
```

### 5.3 scan-result 500

```text
2026-07-04T02:44:57Z POST /wechat/sessions/scan-result 500
psycopg.errors.ForeignKeyViolation:
update or delete on table "wechat_session_bindings" violates foreign key constraint "message_events_binding_id_fkey"
Key (id)=(4ce249c4-a8ba-4519-9558-0f9e2fcc7e64) is still referenced from table "message_events".
```

## 6. 当前现场状态

| 项 | 状态 |
|---|---|
| `read-targets` | `[]` |
| binding | `disabled / paused / allow_listening=false` |
| 停止后新增消息 | 0 |
| Worker 锁 | `message_ingest` 锁已释放；后续可能进入普通 `first_screen_session_scan` 周期锁，不再有目标读取 |

## 7. 下一轮回归要求

1. 先修复停止后仍尾随 `message_ingest` 的问题。
2. 先修复 `scan-result` 外键删除 500。
3. 语音真实转写需要现场发送一条新的语音，例如：“测试语音转文字成功”，然后验证：
   - 新增 `message_type=voice`
   - `content` 为真实可读文本
   - `raw_payload.voice_transcription.raw` 或等价字段有真实转写证据
   - 不得出现 `2"`、`2" ((`、`[语音] 2"` 等时长/乱码内容
