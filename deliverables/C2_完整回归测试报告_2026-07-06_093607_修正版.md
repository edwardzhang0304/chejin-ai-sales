# C2 完整回归测试报告（修正版）

测试时间：2026-07-06 09:31:38 - 09:36:07 CST  
测试对象：C2 Windows 实机 Worker 目标读取、客户/销售语音转文字、消息入库、停止后不尾随、scan-result 稳定性  
Worker：77b5e5b0-c796-4e52-adc7-c17b87816941  
目标短码：CJR8S5K3  
绑定：f4df23de-6a4e-44c3-b641-8b6c6f9cc0c1  
会话：e31966a3-def4-443d-a339-c601b8f15656

## 1. 测试结论

结论：不通过。

本轮目标下发、目标定位、读取流程、`scan-result`、停止后不尾随均通过；但 C2 语音链路仍未通过。需要特别修正验收口径：**销售发出的右侧语音也属于 C2 会话消息采集范围，必须转文字、入库，并正确标记为 `self/sales`，不能跳过。**

本轮核心失败点：
1. 左侧客户语音：现场看到 Worker 打开了右键菜单，但没有点击菜单里的“转文字”。
2. 右侧销售语音：现场看到右侧气泡没有可用右键菜单，销售侧语音转文字未验证通过；这不是符合预期，而是待修复/待适配问题。
3. 后端本轮新增 3 条消息，但新增 `message_type=voice` 为 0。
4. 疑似语音内容被入库为 `customer text`：`の3"\n还很凉快。`，类型错误且正文带语音时长/OCR 前缀。

## 2. 测试范围

| 范围 | 是否执行 | 结果 |
|---|---:|---|
| Worker 在线和心跳 | 是 | 通过 |
| read-targets 返回 CJR8S5K3 | 是 | 通过 |
| 目标定位/读取链路 | 是 | 通过 |
| 客户侧语音转文字 | 是 | 不通过 |
| 销售侧语音转文字 | 是 | 不通过 |
| voice 消息入库 | 是 | 不通过 |
| 语音正文干净入库 | 是 | 不通过 |
| sender_role 区分 customer/self | 部分执行 | 不通过 |
| scan-result 不再 500 | 是 | 通过 |
| 停止后不尾随目标读取 | 是 | 通过 |

## 3. 执行情况

| 北京时间 | UTC 时间 | 事件 | 结果 |
|---|---|---|---|
| 09:31:38 | 01:31:38 | 基线检查 | Worker 在线，目标停用，`read-targets=[]` |
| 09:31:48 | 01:31:48 | 启用 CJR8S5K3 | 绑定 `bound/listening/allow_listening=true` |
| 09:32:08 | 01:32:08 | scan-result | 200 OK，目标绑定更新 |
| 09:32:12 | 01:32:12 | Worker 进入 `target_chat_locating` | 开始目标定位 |
| 09:32:59 | 01:32:59 | Worker 进入 `visible_hit_message_read` | 开始读取可见消息 |
| 09:33:21 | 01:33:21 | Worker 进入 `voice_transcribe_current_chat` | 触发语音处理流程 |
| 09:33-09:34 | 01:33-01:34 | 现场观察 | 左侧语音打开右键菜单但未点击“转文字”；右侧销售语音未出现可用右键菜单 |
| 09:34:06 | 01:34:06 | `/wechat/messages/ingest` | 200 OK，新增 3 条消息，voice=0 |
| 09:34:21 | 01:34:21 | 停止 CJR8S5K3 | 绑定停用 |
| 09:35:23 | 01:35:23 | 停后复核 | `read-targets=[]`，停后新增消息 0 |
| 09:36:05 | 01:36:05 | 最终心跳 | DB 最新心跳 `locked=false` |

## 4. 缺陷统计

| 严重等级 | 数量 | 说明 |
|---|---:|---|
| P0 | 3 | 客户语音未完成转文字；销售语音未处理；语音被错误入库为 text 且正文污染 |
| P1 | 0 | 无 |
| P2 | 0 | 无 |

## 5. 高风险缺陷

### BUG-C2-20260706-03：左侧客户语音右键菜单已打开，但未点击“转文字”

严重等级：P0  
影响范围：客户语音采集、语音转写、C3 上下文准入  

复现步骤：
1. 启用 `CJR8S5K3`。
2. Worker 进入目标会话。
3. 目标会话可见左侧客户语音气泡。
4. 观察 Worker 执行 `voice_transcribe_current_chat`。

实际结果：
- Worker 对左侧客户语音气泡打开了右键菜单。
- 未点击菜单中的“转文字”。
- 后端未新增 `message_type=voice`。

预期结果：
- Worker 应点击“转文字”。
- 转写完成后入库为 `message_type=voice`。
- `sender_role=customer`。
- `content` 为干净转写文本。

### BUG-C2-20260706-04：语音内容被入库为 text，且正文包含时长/OCR 前缀

严重等级：P0  
影响范围：消息类型、业务正文、AI 上下文、后续上报  

实际结果：
- 本轮新增 customer 消息类型为 `text`。
- content 为：

```text
の3"
还很凉快。
```

预期结果：
- 应入库为 `message_type=voice`。
- content 应为：

```text
还很凉快。
```

### BUG-C2-20260706-05：右侧销售语音未被处理，销售侧语音采集未通过

严重等级：P0  
影响范围：销售侧会话上下文、完整消息采集、后续业务判断  

正确验收口径：
- 右侧销售语音也属于 C2 消息采集范围。
- 应执行转文字并入库。
- 需要标记为 `sender_role=self` 或 `sender_role=sales`，不能误标为客户。

实际结果：
- 现场观察右侧气泡没有可用右键菜单。
- 未完成销售侧语音转文字。
- 后端未新增销售侧 voice 消息。

预期结果：
- 右侧销售语音也应转文字。
- 入库为 `message_type=voice`。
- `sender_role` 应为 `self/sales`。
- `content` 为干净转写文本。

## 6. 性能和接口稳定性

| 指标 | 结果 |
|---|---|
| `/wechat/sessions/scan-result` | 本轮均 200 |
| `/wechat/messages/ingest` | 本轮 200 |
| API 500 | 未发现 |
| ForeignKeyViolation | 未发现 |
| 停止后新增消息 | 0 |
| 最终锁 | DB 最新心跳 `locked=false` |

## 7. UAT 验收结果

不通过。

真实业务场景下，客户和销售的语音都会影响会话上下文。当前客户侧语音没有真正点击“转文字”，销售侧语音也没有处理，且疑似语音内容被当作普通 text 入库并带有噪声前缀。该结果不能支撑 C2 验收，更不能进入 C3。

## 8. 遗留问题

| 问题 | 等级 | 当前状态 | 上线影响 |
|---|---:|---|---|
| 客户语音右键菜单未点击“转文字” | P0 | 未解决 | 客户语音无法可靠采集 |
| 销售语音未处理 | P0 | 未解决 | 销售侧上下文缺失 |
| 语音内容入库为 text 且正文污染 | P0 | 未解决 | 下游识别和上报不可用 |
| scan-result 500 | P0 | 本轮未复现 | 继续回归 |
| 停止后尾随 | P0 | 本轮通过 | 继续回归 |

## 9. 上线建议

不建议进入 C3。

下一轮回归准入：
1. 左侧客户语音必须点击“转文字”，并入库为 `voice/customer`。
2. 右侧销售语音必须支持转文字，并入库为 `voice/self` 或 `voice/sales`。
3. 语音 `content` 必须是干净转写文本，不得包含 `3"`、`)3"`、`の3"` 等时长/OCR 前缀。
4. 不允许把语音转写结果降级入库为普通 text，除非有明确字段标记和可验收规则。
5. 继续验证 scan-result 200、停止后无尾随、最终锁释放。

## 10. 当前现场状态

已停干净。

- CJR8S5K3：`disabled/paused/allow_listening=false`
- `read-targets=[]`
- 停后新增消息：0
- DB 最新心跳：`locked=false`
