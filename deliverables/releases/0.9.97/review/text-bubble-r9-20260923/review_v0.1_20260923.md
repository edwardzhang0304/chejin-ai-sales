# 气泡修复及恢复入口 r9 独立复审

2026-09-23，架构师4号。本文件仅为审计证据，不新增项目技术规范。

## 结论

**本次 TBG 候选源码复审通过，r8 遗留的恢复入口 P1 关闭；本轮核查范围内没有发现新的阻断缺陷。** 结论针对 r9 补交后的代码，不是原 r8 已经完整通过，也不是 Windows 正式包、现场恢复或实际发送验收通过。

原问题不是新 OCR 算法又坏了，而是正常发送前入口已接上失败回执，旧任务恢复入口漏传客户编号。r9 同时补齐分段重启续接的同类遗漏，并将公共参数改为必填，未再增加恢复流程。无需为本轮继续扩改 OCR、降低声波门槛或重做状态机。

## 独立核查与执行

候选 `/private/tmp/cj-ocr-bubble-fix-20260923`，基线 `89dbcf097c39076441d5d79caadacfa2ff9848cb` 加未提交差异；共享候选 `/private/tmp/cj-bubble-shared-fix-20260923`，基线 `a2026d1570a84abcda02de9a67844c540bf1aead`。权威文档为主目录四文档 0.9.97/r5，业务规则仍为 TBG-1—7。

| 本轮实际执行 | 结果 | 原始文件 |
|---|---|---|
| 原架构师 pending/running 反例原文件，加五项既有结算检查 | 7通过，退出0，1.86秒 | unit.xml / unit.log |
| pending、running、分段重启 × 请求未到／提交后丢响应 | 6通过，退出0，76.41秒 | recovery.xml / recovery-temp |
| 恢复调用方旧漏传及公共空默认，原正例断言不变 | 3项反向包装通过：均捕获缺少失败回执的预期失败；退出0 | reverse.xml / reverse-temp |
| 原发送前／段间四种断网场景，加去保存／旧错误映射两项反向检查 | 6通过，退出0，53.09秒 | existing.xml / existing-temp |

合计22项检查，其中17项正向、5项反向包装；不是22条独立业务流程，也不与工程师369项或前轮结果相加。使用独立 Python3.12 环境、专用 PostgreSQL、真实 HTTP 和 Worker 子进程。框架弃用及 XML 属性警告保留，所有进程均正常退出。

六个新增场景均由正式异步链自动生成任务；测试没有插入回复任务。pending/running 从正式拉取、claim、`_execute_task` 进入；分段场景由正式批次入口保存续接标记后退出进程，等待原 UI 租约自然到期，经正式 `tick_once` 进入续接。分段第一条已发送事实由真实 HTTP 许可及 sent_ack 建立，**不是微信物理发送**。

恢复用同一 SQLite 和原 Flow，不清库、不手改故障状态。独立核对原始进程输出及数据库断言：失败记录在请求前保存；恢复阶段零桌面读取、零重复发送；任务终态及未发余段取消；无转人工；原发送回执数量保持；原 Flow 清除。随后调用正式 WebChannel 开始处理函数和后台恢复逻辑，后端与 SQLite 均为 running。Qt 渲染、微信界面和模型受控。

这些用例止于恢复 running，**不宣称每个场景都继续发送下一客户**。前轮已有正式完整扫描调度→下一正常任务 completed→sent_ack→Flow 清空的独立证据，见 `/private/tmp/cj-tbg7-scheduler-review-20260923-RB8fPf/review_v0.1_20260923.md`，未冒称本轮重新执行。

## 必查：统一规则、分工及代码质量

1. 对 Worker 全部生产 Python 文件作名称检索和 AST 调用检查：公共结算定义只有一处，`conversation_id` 已为必填；全部9处调用均显式传 `target.conversation_id`。其中旧任务恢复3处、正常批次5处、分段重启1处，见 static.json。原架构反例与固化副本逐字节相同；本轮运行的是原架构文件。
2. 实际读取失败入口 `task_runner.py:8205` 与 `reply_sequence_runtime.py:209` 均进入同一 `_settle_chat_reply_context_failure_before_unlock`，不猜当前微信客户，不另设默认客户。不再将本次技术分组错误替换成旧的转人工错误。
3. OmniAuto 仍只负责原图解析、气泡归属及失败事实。Worker 保存、补交原失败回执并协调原 Flow；后端核验客户／任务／Flow／fencing／发送状态，沿原任务失败及取消未发余段结算。没有 Worker 或后端二次拼字。
4. `reply_read_failure` 是原 Flow 结束回执的依赖，复用已有 SQLite、失败接口、OperationLog 和恢复线程。未增加第二个重试器、队列或状态机。已有普通读取重试证明与本次分组技术失败按原证据适用条件分流，不用通用旧失败码覆盖新事实。
5. 已签发发送许可、sending／sent／unknown 仍由原 sent_ack 处理，不允许读图失败把它们改成“未发送”。这些后端保护和两个回执模块指纹与 r8 独立通过版本一致，r9 未改；不将前轮9项保护检查冒充本轮重跑。
6. Sidecar 和 OCR 适配器在产品内置与共享仓逐字节一致，且与 r6/r8 独立证据指纹一致；没有新增另一套阈值。此次改动是必填参数及调用补齐，未发现需要拆大文件或扩大重构的明确依据。

## 证据与交付边界

- 原 PNG 真实 OCR 和完整正文证据沿用未变源码的 r6 独立报告 `/private/tmp/cj-tbg-r6-review-20260923-Kje2vF/review_v0.1_20260923.md`；语音真实图标→真实 OCR→解析→转写执行的8项独立检查沿用 r8 报告 `/private/tmp/cj-r8-review-20260923-NTZpwC/review_v0.1_20260923.md`。本轮未再运行 OCR，不把旧结果写成本轮新测试。
- 四文档 r5 当前段落已说明实际回执字段、9个入口及未发布边界；历史段落保留当时时点。候选和主目录已登记指纹核对一致；技术方案／版本记录整文件因保留历史不同而不同，不误判为当前设计分叉。
- 7个无声波旧夹具失败、共享顶部裁切存量失败仍不算通过。工程师 Python3.13 曾在断言通过后退出134，原因未确认；本轮 Python3.12 干净退出不能证明那个环境问题已修复。
- 已入库的两条错误历史未改，不承诺升级后原客户自动续聊。Windows 正式联合包、微信实发及销售现场恢复仍待验；本报告不授权提交、推送、打包或部署。

关键 SHA256（全部明细及前后复核见 static.json）：

- task_runner.py：`0a73257d96c670511229742de2165d9ec1204953b29f52f834354fc3924f092b`
- reply_sequence_runtime.py：`61ef3e0a3a74cc37396b04b8f0fe1f43a3b6e13366adf630ebcea4bdd18eaed9`
- Worker reply_read_failure.py：`06ac7be80fa077f02e44542bee60e48ad0b4a2ac94beee9989a0db54835eef54`
- 后端 reply_read_failure.py：`35d66d9337d1229a2276487b6dcad8639c71b14358a1ac6175fc43f3bfd8647b`

git diff --check 通过。审计只新增本目录脚本／报告／测试产物，未改产品、权威文档、事故数据或发布状态。本轮专用数据库容器 `cj-r9-independent-review-20260923`（精确ID `b6e71f8ae1371186edca63560afde31a0c1066001e152d19b846a3ba82a50690`）核对用途标签及匿名卷后已删除；仅清理可重建测试库，XML、日志及 SQLite 保留。其他业务及工程师数据库未动。
