# 0.9.97 气泡拆块 r6 独立复审证据

日期：2026-09-23。审计者：架构师4号。r6 是工程复审轮次，不是应用/合同或权威文档修订号。本文件仅为原始审计证据，不增加项目规范。

## 结论

**已报告的气泡拆块/入口静默漏字 P1 在本轮核查范围内关闭；未发现新的确定代码缺陷。整体交付仍待关键链路补证，不能据此提交发布放行、宣称高磊原客户已经恢复或 Windows 实发通过。**

本轮独立执行了原18项测试、8项新对照、双仓相关回归、两张事故原PNG新鲜真实OCR，以及仅关闭r6入口分支的反向检查。未修改产品源码、原反例断言、现场数据；未提交、推送、构建、部署或操作微信。

## 候选与规则来源

- 产品：`/private/tmp/cj-ocr-bubble-fix-20260923`，HEAD `89dbcf097c39076441d5d79caadacfa2ff9848cb` 加未提交差异。
- 共享：`/private/tmp/cj-bubble-shared-fix-20260923`，HEAD `a2026d1570a84abcda02de9a67844c540bf1aead` 加未提交差异。
- 权威：主目录技术方案 `AI智能客服售前跟进系统_技术方案手册_v0.9.97.md` 的 **r4/TBG-1—7**。候选工作树随附的r2文档不是本轮新增设计依据。正式发布身份不变。
- Sidecar SHA256：`9b6cc1cfbe679423b02abb4ee8c20502b7c6925bc565d82c9a2f732bdc3b99c3`。
- OCR engine SHA256：`6bf6cae49610812f998364e33e3bb1629e8e5481cc4787361e8a3866ec415db2`。
- Worker transaction_outcomes SHA256：`929c4bcc9d9dd8fdfc62474ae2c8cfeac0af4873fe626afa0a4b71d0cf3e3a7c`。
- 产品 frame 测试 SHA256：`aed60c724994a39cf496c025fe38791a5659b83095d00867c458a391e229dbfb`。
- 双仓受影响Sidecar和OCR engine逐字一致；两仓diff检查通过。工作树已有`:memory:.ses`未删除，不应带入正式包。

## 独立结果（不将镜像或重叠数量相加）

| 检查 | 本轮结果 | 原始证据 |
|---|---|---|
| 原6项候选入口＋原4项尾行＋最初8项反例/对照，断言不变 | 18通过 | prior18.xml |
| 独立正常聊天及失败类型对照 | 8通过 | controls.xml / test_controls.py |
| 产品头像/普通文字相关测试 | 76通过，9语音相关未选 | product-nonvoice.xml |
| 共享同范围，不跳过已知边缘失败 | 75通过、1失败，9语音相关未选 | shared-nonvoice.xml |
| 上述共享边缘失败加载HEAD旧Sidecar重跑 | 同一断言仍失败 | shared-head-edge.xml / shared_head.py |
| 10:59、11:16原PNG：真实OCR→正式parser/envelope→observation→JSON→Worker合同校验 | 分别3/4条，全部角色及全文精确断言通过 | original-replay.json / replay_originals.py |
| 仅在内存关闭r6共同相交/0归属分支，原6项不改 | 2项预期失败、4项对照通过；客户/己方均恢复“成功但缺后半句” | entry-off.xml / entry_off.py |

共享失败为`test_invalid_and_ambiguous_evidence_is_not_normal_absence`顶部头像期待ambiguous而实际absent；恢复HEAD旧Sidecar仍失败，相关头像检测器无本轮差异。不能计入通过，也不据此要求改本轮普通文字拼接。两项pytest配置警告未伪装为业务通过。语音未执行，既有7项语音失败没有被本报告关闭。

新8项对照：客户/己方×下一条同角色/不同角色的4例独立上下气泡仍为两条；两例顶部无当前头像的无关碎块不全局停单；两例真实候选入口异常经正式异常封装、清洗、JSON后保留技术失败分类。合成几何和受控OCR框不冒充Windows现场；两张原PNG回放没有使用历史OCR结果或手填消息补齐，仅复用原布局记录。

## 规则统一性与代码质量

1. **入口范围**：`merge_same_line_private_text_fragments`在任何`selected`为空返回前检查候选。完全归属同一气泡则选择；两个已证明独立气泡不合并；一方/双方仅相交但不能完整归属的相关候选明确失败。完全无锚、无关区域继续原排除，不扩大为全屏门禁。2px边界未放宽。
2. **新旧规则接线**：已选物理气泡进入`physical_groups`，跳过旧逐行续接；成员核算消费原始OCR集合而非仅已过滤子集，最终校验使用同一归属证据。未拆块普通文字保留原流程。未发现所测已选气泡重新被旧无锚过滤丢弃。旧入口保留是范围区分，并非Worker/后端另起拼接。
3. **文字来源**：重叠行复用OCR适配模块的整行识字，不用相同首尾字符删字；不重叠拼接保留全部字。真实字形的重复数字/重复词、一次调用/失败缓存、空值及异常分数由本轮产品回归覆盖；没有改历史90分/5分和一次复核规则。
4. **共用入口**：普通读取、发送前刷新/整帧回退、发送后/历史复核实际调用`parse_current_chat_frame_messages`→同一`parse_messages_from_ocr`；媒体窗口直接调用同一parser。段间消费原发送/读取入口，未新增独立拼接实现。本轮对这些连接做代码检索，不冒充每个入口均做过实机操作。
5. **三端分工**：OmniAuto产出完整消息或失败；Worker的`FRAME_TECHNICAL_ERROR_CODES`接新错误，TaskRunner原停单/finally结算选择`technical_failed`；后端`worker_service`原终态要求先faulted、同会话及错误码。媒体窗口保留`vision_window_message_parse_failed`，原机器合同映射为`C2_IMAGE_OBSERVATION_FAILED`，是同技术失败语义的既有包装而非成功兜底。后端未加事后拼消息或猜字规则。
6. **核实边界**：局部失败生产者→异常封装/JSON→Worker分类已实测；故障SQLite、断网补交、后端Flow最终收尾和下单恢复仅核对可达代码，未在r6执行，不能由静态连接推定通过。
7. **代码质量**：本轮同行判据复用公共函数，整行OCR集中于原适配模块，帧内归属只在Sidecar维护，未新造Worker/后端拼接或恢复状态机。未发现需要为本轮继续拆大文件/重做架构的明确理由；不把所有保留的类型分支当作重复规则。

## 尚未满足的TBG-7证据与给11号的下一步

优先补现有设计的验收，不再盲目扩展产品代码：

1. 固定以上r6源码指纹，将本轮新鲜原图输出贯通正式C2 HTTP和隔离PostgreSQL，断言完整角色/正文、重复上报仍只入库/生成任务一次；必须由正式异步回调生成回复任务，关闭回调后正例应失败。禁止测试补任务。受控模型/桌面需标注。
2. 用本轮真实parser错误进入正式Worker，保存同一SQLite；模拟断网后重启同库，自动补交原失败、结清同一Flow，断言未转人工、未重复微信动作。完成后仍faulted；走明确“开始接单”入口，再实际处理下一正常测试任务，不能只assert running。不得清库或手改状态帮助通过。
3. 补媒体包装和发送前/段间受影响入口的定向传递检查，区分普通读取未发送与发送结果未知；不能因解析失败就授权物理重发。原语音遗留失败继续单列，不改断言凑绿。
4. 按原发布验收做Windows正式包/实发，未执行前保持待验。本轮不据此授权打包发布。
5. 已入库错误历史保持不动，原客户恢复单独处理并获授权。本修复证明新读取不再拆错，不证明旧账已自动修好。补证完成时同步权威版本记录和候选文档状态，不把工程r6写成新合同号。

当前没有新代码P1需要继续返工；阻断整轮通过的是上述明确的证据缺项。前轮HTTP/PG结果不计入r6。测试产物仅写入本目录，旧证据保持。
