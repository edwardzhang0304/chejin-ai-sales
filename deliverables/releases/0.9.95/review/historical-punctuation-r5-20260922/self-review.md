# 历史标点与旧故障恢复 r5：工程师补修自审

本轮补修自测通过，待架构师独立复审；未提交、推送或发布。r4原故障补交保留，不能把源码测试写成高磊现场已经恢复。

## 修复与复审边界

r4复审P1已先在原脚本复现：完整历史A/B/C，可见帧B/C，正文完全不变却返回 `historical_text_correspondence_unverified`。本轮只修消息索引对应：发送前传入冻结帧已有身份，公共函数按stable_id/source_message_key取同一历史条目。身份冲突、重复、乱序或投影不符仍拒绝。无需模糊证明的正常后缀直接沿原连续性通过；有正文差异仍走原HC。

本地匹配结果保持可见帧编号，HC证明保留完整权威历史编号及原摘要；没有截短历史、重签检查点、改正文、降90分/5分差距或增加OCR次数。新增直接检查合法HC证明对应 `(1,0)/(2,1)`，而Worker局部匹配为 `(0,0)/(1,1)`，并让共享验证器重新验证原完整证明。

后端产品代码沿用r4，未再变化。恢复测试已固化架构师补验要求：实际Worker先产生原HTTP409并留下故障SQLite；重启恢复仅加载同一绑定、待补交包、账本和故障，不再预写这些状态。输入保留原错误码及一次尝试；原包补交确认、账本清空后，用户显式开始使本地和服务器running。正常和服务器成功但回执丢失均通过，补交零UI操作、无重复微信发送。测试禁用领取新任务，不代表完成下一单。

## 本轮结果

- 车金相关规则与Worker303项通过，含架构师未改动的原反例；共享仓159项通过，两批有重叠。
- 真实HTTP、隔离PostgreSQL、实际Worker进程与SQLite64项通过，包含可见后缀单条/分段回复、媒体、插话、原故障补交与保护。新后缀场景仅改变桌面输入，冻结帧由正式后端自行产生；检查服务端确实从worker-message-2起冻结4条，而完整历史仍保留第一条。没有手工缩短后端检查点或补造HC证明。
- 撤去索引映射，同样10项中5项后缀正例失败、5项完整历史对照通过；同一真实HTTP后缀发送用例重新出现 `C2_PRE_SEND_MESSAGE_SEQUENCE_ALIGNMENT_FAILED`，零回复。这两批是预期反向失败，没有改断言。
- 原事故冻结观测与现场SQLite只读回放仍为96分，原身份和服务器摘要不变。本轮没有重新跑OCR，也没有访问销售电脑或生产数据库。
- 四个随机测试库全部删除并确认不存在；两仓4份本轮共享改动文件逐字一致。原灰度基线、无关已有删除及未跟踪文件保持。

首轮HTTP补充断言误从ReplyAction读取第一段冻结帧，实际权威位置为MessageBatch.ai_request_snapshot；修正断言位置后5项定向检查及64项回归通过。首轮303项中2项因pytest默认临时目录不存在报环境错误，显式指定独立basetemp后303项通过。首次失败日志保留，不计为产品反例。物理微信动作、桌面观测和外部模型仍为受控边界，Windows及联合正式版本待验。

原始工作证据 `/private/tmp/cj-punctuation-fix-20260922/`，本目录XML与源码哈希见 `verification.json`；撤除映射插件同目录已留存。r4审计报告 `/private/tmp/cj-punctuation-review-20260922-ZMVbye/review_v0.1_20260922.md` 的未放行状态由后续独立复审决定，不由此自审关闭。
