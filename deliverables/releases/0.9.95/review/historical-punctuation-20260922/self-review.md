# 2026-09-22 历史标点差异与旧读取恢复：工程师自审

源码修复及定向自测通过；未独立架构复审、未提交、未发布，不能声称高磊现场已恢复。

## 根因与改动

Worker 的旧业务签名忽略标点，把旧价格问题里的逗号/小数点视作同一内容；多个入口以此跳过了 HC 证明，后端原文检查拒绝。随后原 Flow 已结束，待补交原包又被 Flow 凭证拒绝。

一个共享函数负责判定候选正文是否仍有差异；普通读取、发送前、Sidecar、上下文及媒体动作前后沿用该函数和原 HC 证明。候选通过不再等于身份已证实，评分门槛和一次额外读取预算未变。新完成态语音也使用当前 HC；原 v1 冻结证明保留独立回放能力，测试同时断言新旧两种合同解释。

后端沿用原已结束读取补交准入，增加两种历史映射拒绝码；由原权威历史与冻结观测重建缺失证明，再执行原完整校验。没有修改客户端原包或历史事实。原事务记录完整请求哈希和验证对应关系，使回执丢失后的同包重试不受检查点已推进影响；不同包不能复用记录。

## 验证与反例

- 车金规则/Worker 286 项；共享仓 153 项。二者重叠，不相加。末次仅增强断言的 6 项入口检查再次通过，包括低分标点差异仍有原一次额外识别机会。
- HTTP/数据库相关回归39项通过；原兼容恢复与新补交检查39项通过；最终补交、改包保护及真实 Worker 恢复14项通过。分批有重叠，以 XML 为准。
- 通过实际 Worker 先发送一条自方消息，下一轮重读由旧比较造成真实 HTTP 409、写入待补交 SQLite、关闭 Flow；之后同一数据库补交并明确开始接单，服务端和本地均为 running。普通和回执丢失两例都通过，原消息只发送一次。没有手工补 HC 证明、伪造入库成功或直接标记本地 confirmed。桌面动作和外部模型是受控替身，HTTP、PostgreSQL、SQLite、队列及恢复逻辑是真实执行。
- 实际事故观测回放得96分，消息身份保持一致。诊断中的检查点经过过滤和小写化；仅使用现场已确认 SQLite 内容恢复字段，恢复后要求与原服务器检查点摘要完全相同。不是重签构造的旧证据。关闭正文检查后同一输入再次缺少证明。
- 禁用共享正文检查，同样9项中8项按预期失败、1项不相关对照通过；禁用已结束映射读取准入，同一补交正例再次报 WORKER_INFLIGHT_FLOW_MISMATCH。反向检查只是撤掉本次修复，未改断言。
- 未决发送、错误身份、变化的读取代次、无关失败、低分、篡改正文/映射/新消息仍拒绝。失败事务没有持久化成功审计；所有随机临时测试库清理并检查不存在。

自测曾真实发现补交成功后回执丢失仍卡住；已将验证对应关系纳入原事务回执，重跑原失败用例通过。首轮 Sidecar 测试缺少合法基线属于测试构造错误，已补正；不把该首次失败算作产品反例。

## 边界

未操作销售电脑或生产数据，未触发真实模型或 Windows 微信发送。两份共享实现逐字一致，原灰度分支历史保留；已有无关删除和未跟踪文件未处理。补交成功不自动接单，仍需用户点击。联合后端/Worker/共享组件版本及 Windows 现场仍须验收。

可追溯清单：`/private/tmp/chejin-customer-interrupt-20260916/deliverables/releases/0.9.95/review/historical-punctuation-20260922/verification.json`。
