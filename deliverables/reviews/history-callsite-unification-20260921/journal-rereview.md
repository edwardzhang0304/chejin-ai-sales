# 未触发发送的准备帧恢复：局部复审交接

2026-09-21。本轮仅处理架构师确认的 Journal 冲突，不扩大媒体改造，不提交、推送、升版本或部署。原待审历史统一补丁保留，正式工作区和业务数据未改。

## 原因及修复

原程序允许 `not_attempted` 意图重新进入原任务，恢复又必须读取新截图；但发送前把新旧 `pre_action_identity_sequence` 整份比较，连本帧编号也要求相同。旧消息仅上下移动就触发 `C3_SEND_IDENTITY_JOURNAL_CONFLICT`。此前把它排除为“冻结请求真实性检查”不充分，原自审遗漏由开发承担。

本次在原发送调用点使用 `refresh_unattempted_send_journal`：当前业务上下文仍先经过原重新读取、历史 HC、事实 checkpoint 和发送许可；Journal 不再另建一套历史正文/编号判断。只有原动作、会话、预留身份不变，已有任务、Flow 和回复正文绑定不变，且日志与条目明确未触发、没有已形成结果或未决启动，才原子更新准备帧。

首次准备证据保留在 `original_send_preparation`，当前准备继续放原字段，不无限追加准备历史。动作状态、条目、启动尝试和资料包引用不重置。已触发/已确认/结果未知仍由原回执恢复处理，不能重发。写入失败向上传播，进入微信动作前停止，磁盘旧记录保留。没有新 OCR、没有扩展恢复框架，也没有修改后端和共享组件的业务代码。

旧 Journal 没有后加的 setup_context 时，仍核对它已保存的原动作/会话/预留身份，使用当前合法任务及发送许可；不会要求旧版本保存不存在的字段。冻结回执不重写。当前截图中客户新消息、销售接管、授权撤销仍由原前置路径决定，不由这个日志更新函数放行。

## 定向验证

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| 架构师原重启反例及不移动对照 | 2 通过 | 真实 Worker、SQLite、Journal，子进程 `os._exit(71)`，同库/同 Flow 新 Runner；只推进租约时钟，不清库或手工结束 Flow |
| 日志更新与原发送保护 | 23 通过 | 包含新更新函数的 8 项检查、现有 Journal 11 项及原 Worker 4 项；错误身份/正文、已触发/已确认、未决子进程拒绝，原子写失败可恢复，未知回执不重发 |
| 换回 HEAD 原发送方法 | 1 通过、1 预期失败 | 位移反例在“应进入发送一次”断言失败，实际零发送、错误码仍为 `C3_SEND_IDENTITY_JOURNAL_CONFLICT`；不移动对照通过 |

两组正向检查合计 25 项，不与原 319/56 项相加。本次只复验这个实际冲突及直接受影响的保护；原 HTTP/数据库、真实 OCR 证据仅复用其原范围，未重跑全量。

重启回归纳入正式测试 `test_prepared_send_restart.py`，沿用独立原探针的业务断言，另外在发送调用入口记录真正落盘的准备日志，确认使用新帧序列并保留原序列。最初未记录该入口的两次执行日志也保留，最终记录以 `probe.log` / `ablation.log` 为准。

这个探针通过真实 Sidecar 解析器从受控 OCR 行生成新本帧 ID；**没有真实 OCR 或 Windows 点击发送，API/桌面受控，没有新 HTTP/PostgreSQL 联测**。FakeBridge 后续完整 AI 气泡证明不足，已有 `AI_REPLY_STABLE_IDENTITY_UNCONFIRMED` 日志不算本次缺陷，2 项不能冒充发送后全链路闭环。保留 fork 多线程警告；两个子进程都到达预设退出点。未创建测试数据库或调用外部模型。

## 收敛范围及复审要求

按用户最新指示，以实际调用链明确冲突为主，不继续为每个比较表达式扩展测试。媒体文字确认计数仍是有附加触发条件的核对项，没有当成普通聊天必然发生的故障，也没有修改该分支。调用清单已更正对应条目。

请架构师4号重点复核：当前业务上下文授权与日志更新是否衔接，旧动作真实性与未知禁止重发是否保留，原反例是否能通过、撤回修复是否确实失败。不要用此次局部通过代替整个候选/Windows验收。用户此前要求对声明覆盖范围内的审计遗漏负责；开发实现与自测责任仍由10号承担。

主候选：`/private/tmp/chejin-history-callsite-unification-20260921`，HEAD `ac288680eb2fa932be72c51642a58f605105e0d9` 加未提交候选。共享候选：`/private/tmp/omniauto-history-callsite-unification-20260921`，HEAD `934c641920fdd4747d679e88fdbb148d4a446ba7` 加原未提交候选，本次未变。

当前源码、测试、精确命令、日志、原反例来源及哈希：`/private/tmp/chejin-history-callsite-audit-20260921/journal-revision/verification.json`。之前整轮证据保留，当前复审请以本次源码登记为准。原独立审计 `/private/tmp/cj-message-rules-independent-20260921-8fs590/review.md` 未改。
