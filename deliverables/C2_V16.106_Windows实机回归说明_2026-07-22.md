# C2 V16.106 Windows 实机回归说明

## 交付物

- 安装包：`chejin-worker-client-20260722-v16.106.zip`
- 客户端提交：`fdfc4d5`
- SHA256：`ba68456591b83e498a9ff9fc1d92a6dd42ba07c4d0abbd01ccc1e2bbda8c17f3`
- 当前 API：`http://192.168.2.106:8000/api`
- 稳定回滚包：`chejin-worker-client-20260721-v16.104.zip`

## 本次主要修复

1. C2/C3 的目标身份统一为短码 `remark_code`，组合名不再参与目标选择。
2. 当前右侧标题已经是目标短码且为 private 时，直接复用当前会话，不再重复点击左侧列表。
3. 当前右侧标题不是目标短码时，不再误判成目标会话 `unknown` 并提前退出，继续按“首屏候选 -> 短码搜索”定位。
4. 只有已经确认标题包含目标短码，但类型为 group/unknown 时才安全终止。
5. `rpa_session_key` 只用于物理会话行冲突证据；旧 key 漂移不能改变读取、点击或回复目标。
6. 回复发送也只用短码定位，并继续强制检查“短码正确 + private”。
7. 没有新增截图、OCR、点击、接口或数据库迁移；文字、语音、V3、去重、停止和图片冻结边界不变。

## Windows 启动命令

以下命令可整段复制到 PowerShell：

```powershell
cd $env:USERPROFILE\Desktop
Expand-Archive -Force .\chejin-worker-client-20260722-v16.106.zip .\chejin-worker-client-20260722-v16.106
cd .\chejin-worker-client-20260722-v16.106\worker-client
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
$env:CHEJIN_API_BASE_URL="http://192.168.2.106:8000/api"
$env:CHEJIN_RPA_MODE="real"
$env:CHEJIN_C2_VOICE_TRANSCRIBE_MAX_DURATION_SECONDS="240"
.\.venv\Scripts\python.exe -m chejin_worker_client.main
```

## 本轮重点测试

1. 客户端必须显示 `V16.106`。
2. 微信当前已打开 `CJR8S5K3` private 单聊时开启监听：不得再次点击左侧同一会话，不得出现右侧聊天区空白。
3. 微信当前停在其他会话、目标短码在首屏时：应只点击唯一目标一次并进入正确会话。
4. 微信当前停在其他会话、目标不在首屏时：应进入短码定向搜索，并点击唯一 private 结果。
5. 当前其他会话标题无法完整识别时：不能直接报目标会话 unknown；仍应进入后续定位。
6. 目标短码群聊与单聊同时存在：只允许 private 单聊；群聊不得读取、转写、入库或回复。
7. 两个 private 单聊使用同一短码：必须判冲突并全部不点击。
8. 左侧名称有空格、OCR 省略号或标点抖动时：只要短码一致，仍视为同一业务目标，不得重复绑定或重复点击。
9. customer/self 文字与语音、V3 顺序、去重、停止监听做一轮基本回归，确认本次身份修复未影响已有链路。

## 已完成自动化验证

- 源码及解压包 Worker：248 passed，29 skipped。
- OmniAuto compatibility：204/204。
- 交互证据：5/5。
- 输入节奏：6/6。
- 窗口规划：28/28。
- mock E2E：通过。
- 后端身份与冲突定向：3/3。
- ZIP 顶层仅 `worker-client/`，`forbidden_entries=[]`。

Windows 实机失败时，请保留失败发生前后精确时间范围，再按该范围打包日志与 artifacts。
