# 车金 Worker 客户端版本管理文档

更新时间：2026-06-30

## 结论

后续所有 Worker 客户端源码包统一使用以下命名规则：

```text
chejin-worker-client-[日期]-v[版本号].zip
```

示例：

```text
chejin-worker-client-20260618-v12.zip
```

不再使用以下非标准命名：

```text
chejin-worker-client-formal-ui-source-20260618-v9.zip
chejin-worker-client-formal-ui-source-20260618-v5-omniauto-speedfix.zip
chejin-worker-client-formal-ui-source-20260618-v5.1.zip
```

## 命名规则

| 字段 | 规则 | 示例 |
|---|---|---|
| 固定前缀 | 必须为 `chejin-worker-client` | `chejin-worker-client` |
| 日期 | 打包日期，格式 `YYYYMMDD` | `20260618` |
| 版本号 | 正式递增使用 `v数字`；已发版本的小范围候选修订可使用 `v数字.数字` | `v12` / `v15.1` |
| 后缀 | 固定 `.zip` | `.zip` |

完整格式：

```text
chejin-worker-client-YYYYMMDD-vN.zip
chejin-worker-client-YYYYMMDD-vN.N.zip
```

## 版本递增规则

每次重新交付给 Windows 实机测试、测试工程师或架构师验收，都必须递增版本号。

以下情况必须递增版本：

| 场景 | 是否递增 | 说明 |
|---|---:|---|
| 客户端 UI 改动 | 是 | 包括窗口、任务链路、截图预览、滚动条、设置页等 |
| Worker 接口/任务模型改动 | 是 | 包括 pull 字段、步骤上报、结果上报 |
| OmniAuto RPA 代码改动并重新打进客户端包 | 是 | 即使客户端 UI 没变，也必须递增 |
| 依赖或打包清单变化 | 是 | 包括 requirements、PyInstaller、内置资源 |
| 只改文档，不重新发客户端包 | 否 | 文档版本单独管理 |
| 只在本地临时调试，未交付测试 | 否 | 不形成正式包 |

同一天已有 `v12`，下一次客户端交付包就是：

```text
chejin-worker-client-20260618-v13.zip
```

跨天后版本号是否延续，由项目统一口径决定；当前建议继续全局递增，避免 `v1` 重复造成混淆。

## 包内容要求

正式 Worker 客户端源码包必须包含：

```text
worker-client/
worker-client/chejin_worker_client/
worker-client/chejin_worker_client/web_assets/
worker-client/tests/
worker-client/run_checks.py
worker-client/smoke_e2e.py
worker-client/requirements.txt
worker-client/omniauto-rpa/
```

正式包不应包含：

```text
.venv/
__pycache__/
.pytest_cache/
.git/
runtime/
临时测试输出
本机 artifacts
frontend/
deliverables/
```

## 出厂状态与本地绑定缓存红线

Worker 客户端安装包出厂状态必须满足：

```text
未绑定
无 Worker Token
无 worker_id
无 client_instance_id
```

首次启动规则：

```text
首次启动必须展示绑定页。
用户输入 Worker ID / Worker Token 后，才允许在本机本地保存绑定信息。
退出重启后，可以从本机缓存恢复绑定状态。
```

重新发包红线：

```text
重新打包或重新交付测试包时，绝不能带入当前开发机、测试机或任意真实机器的本地绑定缓存。
正式包不得包含 runtime、local storage、SQLite 本机数据、client_instance_id、worker_id、Worker Token 或任何机器级缓存。
```

后续每次发包和测试准入都必须检查该项。若发现安装包内带机器缓存、Token 或绑定状态，直接判定为发包不合格，需要重新清包并递增版本重新交付。

## 当前统一验收基线

当前 Worker 客户端统一验收基线版本为：

```text
/Users/zhangwentao/Documents/车金/deliverables/chejin-worker-client-20260622-v15.3.zip
```

版本口径：

```text
v15.2 Worker/UI/API/RPA 能力 + 搜索按钮点击后延迟截图等待搜索结果稳定 = v15.3
```

验收口径：

```text
v15.3 已完成 Windows 实机验收，作为后续新增需求和问题修复的统一起点。
用户不再单独补写验收报告；后续以本版本管理文档、项目总目录和用户确认口径作为基线依据。
```

后续版本递增规则：

```text
后续客户端新增需求或问题修复从 v16 开始递增，后续依次为 v17、v18……
不再使用 v15.4、v15.5 作为后续开发版本号。
```

## 2026-06-24 v0.6 口径修订

结论：v16.1 中关于“主动扫描 + 补偿扫描 + 去重入库”的旧描述已废弃。当前 C2/C3/C4 统一按技术方案 v0.6 和测试口径调整说明执行：

```text
C2 = 第一屏主动扫描 + 第一屏命中优先读取 + 状态机定向读取 + 去重入库
C3 = 发送前必须 pre_send_refresh，客户有新消息时旧 reply_action=superseded，不发送旧回复
C4 = 召回到期先 recall_precheck，读取微信事实后再决定是否 follow_up
watching 不作为主状态
```

以下内容不再作为当前版本开发、测试或验收依据：多屏滚动补偿扫描、第二屏/第三屏滚动扫描、sessions compensation、滚动页数、滚动到底判断、滚动恢复位置、补偿扫描任务、补偿扫描验收项。

## 版本记录

| 版本 | 包名 | 状态 | 主要内容 |
|---|---|---|---|
| v5 | `chejin-worker-client-formal-ui-source-20260618-v5.zip` | 历史包 | 早期可运行客户端源码包，曾作为稳定 UI 参考；旧命名，不再作为后续标准 |
| v6 | `chejin-worker-client-formal-ui-source-20260618-v6.zip` | 历史包 | 客户端 UI/源码包迭代版本；旧命名，具体差异【待确认】 |
| v7 | `chejin-worker-client-formal-ui-source-20260618-v7.zip` | 历史包 | 客户端 UI/源码包迭代版本；旧命名，具体差异【待确认】 |
| v8 | `chejin-worker-client-formal-ui-source-20260618-v8.zip` | 历史包 | 任务链路截图缩略图可点击弹出大图；不显示本地路径；不增加“打开证据”入口；滚动条/滑块改为细圆角样式；补 UI 合约测试 |
| v9 | `chejin-worker-client-20260618-v9.zip` | 历史包 | 继承 v8 客户端 UI；打入最新 OmniAuto RPA：搜索框 OCR/视觉优先定位、旧手机号残留清空复核、手机号精确 OCR 校验、固定兜底红色报告提示、balanced 等待策略、确定后默认跳过严格 OCR |
| v10 | `chejin-worker-client-20260618-v10.zip` | 历史包 | 继承 v9；优化 Worker 客户端红/绿状态指示灯为独立呼吸动画组件；去掉状态文案里的静态 `●` 字符；补 UI 合约测试防退化 |
| v11 | `chejin-worker-client-20260618-v11.zip` | 历史包 | 继承 v10；OmniAuto 报告中 `window_layout_calibration` 单独生成微信窗口布局分区标注图，和 `entry_before_capture` 的入口定位图分离；确认 add_friend 链路已启用悬浮球键鼠守护并默认传入 `--block-manual-input` |
| v12 | `chejin-worker-client-20260618-v12.zip` | 历史包 | 继承 v11；Worker 在正式字段校验通过后、调用 OmniAuto 前立即上报 `启动 OmniAuto`、`检测微信窗口`、`启动键鼠守护` 三个前置步骤，减少领取任务后界面空等 |
| v13 | `chejin-worker-client-20260618-v13.zip` | 历史包 | 继承 v12；按 `worker-workbench.html?v=20260618j` 设计稿收敛 Worker 客户端全页面：绑定页、暂停空态、接单等待、运行中、完成、失败、离线、设置、接单时段、日志；窗口尺寸回到 `316x628`、状态区改为整体 2x2 容器、底部接单区改为内嵌圆角操作条、设置页行高/图标/开关/日志表格按设计稿收敛；补 UI 合约测试防退化 |
| v14 | `chejin-worker-client-20260619-v14.zip` | 历史可回退 | 继承 v13 Worker/RPA/API 能力；默认 UI 入口切到 `chejin_worker_client/web_ui.py`，使用 PySide QtWebEngine + QWebChannel 承载 `packages/worker-ui-baseline` fix11 构建后的 `web_assets`；Worker 客户端 UI 单一源头已收敛到 `packages/worker-ui-baseline`，`frontend` 不再维护 Worker 客户端预览，`worker-client/chejin_worker_client/web_assets` 只作为构建产物；修复 WebView 窗口拖动，改为页面标题栏通过 QWebChannel 主动移动原生窗口；修复设置页返回、圆角裁剪、底部 dock 贴底、状态 pill 换行、状态按钮垂直居中、绑定信息 fallback、离线状态口径混乱、无任务卡片误显示 add_friend、长 Worker ID 撑爆状态卡、失败/完成页底部操作栏不跟随真实接单状态更新，并 pin `numpy<2` 避免 Windows 端 PySide/shiboken 与 NumPy 2 冲突；同步 fix11 字号约束：销售名 `18px/24px`，窗口内部不使用 `20px+` 字号，带任务链路页面保留底部操作栏安全区，接单时段设置页时间范围独占一行；接单时段设置已从静态稿改为可用功能，自动接单时段开关和开始/结束时间写入本地 SQLite，关闭时不限制时段，开启时仅在设定时间内领取新任务，非接单时段保持心跳但不 pull task；绑定、开始接单、暂停接单、设置/日志导航仍回到原 Python 业务层；`TaskRunner`、`WorkerApiClient`、任务状态机和 RPA 桥接不重写。压缩包为干净 `worker-client/` 单根目录，不包含 `frontend` 或 `deliverables`。 |
| v15 | `chejin-worker-client-20260620-v15.zip` | 上一测试包 | 基于 v14；接入 OmniAuto 最新 upstream `cafa57f` 后的 V15 路由收口分支 `worker-v15-add-friend-windows-route-only`，集成 commit `32c9ebfcd34cd217487a4e01984e5f054754c683`，PR：`https://github.com/meta-xucong/omniauto/pull/21`；Worker 正式 add_friend 入口只保留 `add-friend-entry-click-plan-windows`，删除/不打入 `add-friend-entry-click-plan` 和 `add-friend-entry-click-plan-windows-1080p-reference`；Worker 业务层、后端接口、任务状态机、UI 展示层保持 v14 口径不变；内置 `worker-client/omniauto-rpa` 改为最小运行副本，只包含微信客服 RPA 运行所需源码/脚本/测试，不打入 OmniAuto `.git`、runtime、历史 docs、data、venv、cache；本地 OmniAuto add_friend smoke 35 项通过，Worker staging 包 `run_checks.py` 40 个 unittest + mock RPA smoke 通过；Windows 真实微信 RPA 待实机复测。 |
| v15.1 | `chejin-worker-client-20260622-v15.1.zip` | 上一测试候选包 | 基于 v15；按 fork-first 规则先同步 `edwardzhang0304/omniauto` 到上游 `meta-xucong/omniauto`，再从用户 fork 拉取本地，集成 fork `master` commit `641b267f7ede163a8c580e75106d5b73edf1a972`（`Stabilize WeChat recorder semantic export`）；Worker UI、后端接口、任务状态机、RPA 桥接入口保持 v15 口径不变，正式 add_friend 入口仍只使用 `add-friend-entry-click-plan-windows`；内置 OmniAuto 最小运行副本同步最新微信 OCR/RPA 与客服链路代码，包内不包含 OmniAuto `.git`、runtime、data、venv、cache，仅补入 add_friend smoke 契约要求的单个文档 `docs/add_friend_rpa_pr_readiness_20260616.md`；OmniAuto add_friend smoke 35 项通过，Worker staging 包 `run_checks.py` 40 个 unittest + mock RPA smoke 通过；zip 结构校验通过，顶层只有 `worker-client/`；Windows 真实微信 RPA 待实机复测。 |
| v15.2 | `chejin-worker-client-20260622-v15.2.zip` | 上一测试候选包 | 基于 v15.1；仅调整 Worker 顶层 `requirements.txt`，将 `Pillow==10.4.0` 放宽为 `Pillow>=10,<13`，与内置 OmniAuto add_friend 依赖保持一致，修复 Windows pip 在当前源/解释器组合下找不到 `Pillow==10.4.0` 导致安装中断的问题；Worker UI、后端接口、任务状态机、RPA 桥接入口和内置 OmniAuto commit 均保持 v15.1 口径；OmniAuto add_friend smoke 35 项通过，Worker staging 包 `run_checks.py` 40 个 unittest + mock RPA smoke 通过；zip 结构校验通过，顶层只有 `worker-client/`，不包含 `.git`、runtime、data、venv、cache、dist；Windows 真实微信 RPA 待实机复测。 |
| v15.3 | `chejin-worker-client-20260622-v15.3.zip` | 当前统一验收基线 | 基于 v15.2；仅调整 OmniAuto add_friend Windows 搜索结果截图时机：点击“搜索”后、捕获 `add_friend_search_result_window` 前新增可配置稳定等待 `WECHAT_WIN32_OCR_ADD_FRIEND_SEARCH_RESULT_SETTLE_MS`，默认 1800ms，上限 8000ms，并在 timings 中记录 `after_add_friend_search_button_click_result_settle_wait`，用于缓解搜索结果区尚未刷新完成就 OCR 导致 `query_search_result` 误判的问题；Worker UI、后端接口、任务状态机、RPA 桥接入口保持 v15.2 不变；OmniAuto add_friend smoke 35 项通过，Worker `run_checks.py` 40 个 unittest + mock RPA smoke 通过；zip 完整性和结构扫描通过，顶层只有 `worker-client/`，不包含 `.git`、runtime、data、venv、cache、dist；已完成 Windows 实机验收，后续新增需求或问题修复从 v16 继续。 |
| v16 | `chejin-worker-client-20260623-v16.zip` | 上一 C2/C3 测试包 | 基于 v15.3 已验收加好友基线继续开发；版本号不再使用 v15.4。新增 Worker 本地 Local WeChat UI Lock，运行态文件为 `runtime/worker/ui_lock.json`，包含 `lock_id / fencing_token / lease_expires_at / operation_type / current_step`，支持获取、续租、释放和过期锁恢复；`add_friend` 执行链已接入本地 UI 锁；Worker 心跳只上报技术方案 v0.4 正式字段；新增 C2 会话扫描与消息读取联调能力，调用 OmniAuto `sessions/messages`，对接后端 `scan-result / read-targets / messages/ingest`；新增 C3 Worker 发送链路，`chat_reply` 任务必须先调用 `claim-send`，成功后再发送服务端批准的 `reply_text` 并上报 `sent_ack`；包内自检 `run_checks.py` 50 个 unittest + mock RPA smoke 通过；C2 已完成 Windows Worker 实机联调，C3 发送链路仍需 Windows 微信实机回归。 |
| v16.1 | `chejin-worker-client-20260623-v16.1.zip` | 历史 C2/C3 测试包，补偿扫描口径已废弃 | 基于 v16 增加独立 C2 listener、Local WeChat UI Lock、read-targets 定向读取、messages/ingest 去重入库和 C3 `claim-send -> send -> sent_ack` 链路；其中关于“主动扫描 + 补偿扫描 + 去重入库”、启动补偿扫描、周期补偿扫描、滚动边界参数和补偿扫描验收限制的描述已被 2026-06-24 v0.6 口径废弃。当前开发和测试只按第一屏主动扫描、第一屏命中优先读取、状态机定向读取、pre_send_refresh、recall_precheck 和 `watching` 不作为主状态执行。 |
| v16.2 | `chejin-worker-client-20260629-v16.2.zip` | 废弃包，不可交付 | 基于 v16.1 和 2026-06-24 技术方案 v0.6 口径继续收口；Worker 客户端显示版本更新为 `V16.2 · Worker C2/C3 客户端`；C2 定向读取以 `conversation_id + remark_code` 做身份收口，`remark_code` 进入 `WechatReadTarget`、日志 evidence 和消息 dedupe_basis；`row_fingerprint` 降级为辅助 evidence，不再作为必填阻塞条件；Worker `run_checks.py` 64 个 unittest + mock RPA smoke 通过，后端 C2 回归 15 passed。但该包误包含 `omniauto-rpa/.../configs/postgres.local.env` 本地 PostgreSQL DSN 配置，违反出厂包红线，已废弃，不允许交付或测试。 |
| v16.3 | `chejin-worker-client-20260629-v16.3.zip` | 历史 C2/C3/C4 测试包 | 基于 v16.2 修复发包边界问题；Worker 客户端显示版本更新为 `V16.3 · Worker C2/C3 客户端`；保留 v16.2 的 C2/C3/C4 字段收口和定向读取规则；新增源码包构建脚本 `worker-client/scripts/build-source-package.py`，硬过滤 `.env`、`*.local.env`、runtime、cache、venv、dist、嵌套 zip 等本地状态和敏感配置；重新打包后 manifest 确认无 forbidden entries；Worker `run_checks.py` 65 个 unittest + mock RPA smoke 通过，包内 `run_checks.py` 65 个 unittest + mock RPA smoke 通过；已被 v16.4 替代。 |
| v16.4 | `chejin-worker-client-20260630-v16.4.zip` | 历史 C2/C3/C4 测试包 | 基于 v16.3 接入 OmniAuto fork V16 分支 `worker-v16-omniauto-upstream-20260630` commit `1a4e0f3` 的 RPA 侧更新；同步 Win32/OCR sender role 识别、微信窗口 DPI/尺寸归一化、加好友搜索结果稳定等待等 RPA 文件；正式 add_friend 入口仍为 `add-friend-entry-click-plan-windows`，不恢复旧入口；C2 仍按第一屏主动扫描、第一屏命中优先读取、状态机定向读取和去重入库，不恢复多屏补偿扫描；C3 发送前仍执行 `pre_send_refresh`，unknown 不自动重发；删除包内历史遗留 `configs/postgres.local.env`；Worker 客户端显示版本更新为 `V16.4 · Worker C2/C3 客户端`；Worker `run_checks.py` 65 个 unittest + mock RPA smoke 通过，OmniAuto add_friend smoke 35 项通过，manifest 确认无 forbidden entries；Windows 微信实机需重点复测 sender_role、DPI 定位、加好友主链路、chat_reply 不重复发送。 |
| v16.5 | `chejin-worker-client-20260630-v16.5.zip` | 历史 C2/C3/C4 测试包 | 基于 v16.4；按技术方案修正 C2 定向读取：第一屏可见目标仍走 visible 快速读取，但读取前必须二次确认 `remark_code`；非第一屏状态机定向读取、C3 `pre_send_refresh`、C4 `recall_precheck_read` 均改为 OmniAuto `messages --target-mode search_by_remark_code --remark-code <短码>`，通过微信搜索框粘贴短码、等待搜索结果稳定、OCR 唯一候选、点击会话、标题/备注短码二次确认后才允许读取 messages；目标未确认、搜索无匹配/多义、OCR 无结果或标题不含短码时失败，不读取当前窗口消息；Worker 桥接层不再在非第一屏读取时传 `rpa_session_key` 作为定位依据；侧车输出 `targeting / step_events / evidence` 便于 Windows 回归和后续 OmniAuto PR；客户端显示版本更新为 `V16.5 · Worker C2/C3 客户端`；Worker `run_checks.py` 66 个 unittest + mock RPA smoke 通过，Win32/OCR window action planning 13 项、window metrics 6 项通过；Windows 回归显示已能搜索 `CJWIN01`，但未点击打开联系人结果，P0 未关闭，已被 v16.6 替代。 |
| v16.6 | `chejin-worker-client-20260630-v16.6.zip` | 历史 C2/C3/C4 测试包 | 基于 v16.5；修复 Windows 搜索结果卡在 `CJWIN01` 候选的问题：OmniAuto `search_by_remark_code` 优先解析微信搜索浮层“联系人”区块，排除群聊/更多/网络结果中的 `包含:CJWIN01` 干扰，命中唯一联系人后点击姓名/行主体多个安全点，并在每次点击后用标题/短码二次确认，确认成功后才读取 messages；左侧客户文本气泡不再上报 unknown，改为 `sender_role=customer`；Worker message ingest 映射支持 `voice/audio/video/file/unknown`，voice/audio 即使无文字内容也不被客户端过滤；绑定页长错误信息增加换行和滚动保护；客户端显示版本更新为 `V16.6 · Worker C2/C3 客户端`；Worker `run_checks.py` 67 个 unittest + mock RPA smoke 通过；已被 v16.7 替代。 |
| v16.7 | `chejin-worker-client-20260630-v16.7.zip` | 废弃包，不可交付 | 基于 v16.6；按 fork-first 规则先将 `meta-xucong/omniauto` 最新 `master` 同步到 `edwardzhang0304/omniauto`，再从 fork 拉取本地；OmniAuto fork `master` 已同步到 `a7257fa Standardize WeChat OCR window sizing (#24)`，本地 V16 分支合入后为 `5133212`；同步 RPA 侧窗口尺寸标准化；但 Windows 实机发现 C2 `search_by_remark_code` 在搜索框已有残留时未强制清空和确认，可能把短码拼接成 `CJWCJWIN01WIN01` 并继续搜索，存在错误输入风险，已废弃并由 v16.8 替代。 |
| v16.8 | `chejin-worker-client-20260630-v16.8.zip` | 历史 C2/C3/C4 测试包，不建议继续测 | 基于 v16.7；修复 C2 定向读取搜索框残留导致短码拼接的问题：点击搜索框后必须 OCR/视觉确认搜索框激活；执行 `Ctrl+A + Backspace` 清空；清空后必须再次确认搜索框仍激活且查询词为空；粘贴短码后必须 OCR 确认搜索词与 `remark_code` 完全一致；若出现 `CJWCJWIN01WIN01` 这类拼接、空 OCR、焦点丢失或搜索框未清空，立即失败并禁止点击候选、禁止读取当前窗口消息；保留“唯一命中联系人后点击客户、标题/备注二次确认短码、再读取 messages”的定向流程；客户端显示版本更新为 `V16.8 · Worker C2/C3 客户端`；Worker `run_checks.py` 67 个 unittest + mock RPA smoke 通过，包内 `run_checks.py` 67 个 unittest + mock RPA smoke 通过，Win32/OCR window action planning 15 项通过；manifest `forbidden_entries=[]`；Windows 回归仍出现 `TARGET_NOT_CONFIRMED_FOR_MESSAGES`，且当时缺少类似 add_friend 的步骤报告，已由 v16.9 替代。 |
| v16.9 | `chejin-worker-client-20260630-v16.9.zip` | 历史 C2/C3/C4 诊断包 | 基于 v16.8；C2 `messages search_by_remark_code` 定向读取补齐类似 add_friend 的步骤报告：每次定向读取都会在 artifact 目录输出 `wechat_messages_targeting_review.html/json`，覆盖字段校验、微信预检、基线截图、搜索框清空、短码粘贴、唯一候选、点击联系人、标题/短码二次确认和最终结论；Worker 日志在 `c2_message_read_sidecar_failed` 中记录 `artifact_dir / review_path / evidence_path / target_mode / targeting / step_events / open_chat_timing`，便于 Windows 现场判断卡在搜索、点击还是二次确认；保留 v16.8 搜索框强清空和查询词完全一致校验；后端修复 C2 `scan-result` stale binding 外键问题，不再物理删除已被 `message_events` 引用的 `wechat_session_bindings`，改为 `disabled + deleted_at` 软退役并归档 `rpa_session_key`；客户端显示版本更新为 `V16.9 · Worker C2/C3 客户端`；Worker 源码 `run_checks.py` 67 个 unittest + mock RPA smoke 通过，包内 `run_checks.py` 67 个 unittest + mock RPA smoke 通过，后端 C2 回归 19 passed，Win32/OCR window action planning 15 项通过；manifest `forbidden_entries=[]`；该包主要用于诊断，搜索结果点击坐标问题已由 v16.10 修复。 |
| v16.10 | `chejin-worker-client-20260630-v16.10.zip` | 历史 C2/C3/C4 测试包 | 基于 v16.9；实际修复 C2 非第一屏定向读取“搜索到 CJWIN01 但未打开联系人”的高风险点：搜索结果候选来自 OCR 截图坐标，点击时改用与 add_friend 一致的 `human_window_image_click` 窗口截图坐标点击，不再误用 `human_client_click` client 坐标；步骤报告的点击尝试中记录 `click_method=human_window_image_click`，便于 Windows 现场确认；新增合同测试 `test_search_result_candidate_uses_window_image_click_coordinates`，确保搜索结果候选不会再走 client 坐标点击；保留 v16.9 的 `wechat_messages_targeting_review.html/json` 报告和后端 stale binding 软退役修复；客户端显示版本更新为 `V16.10 · Worker C2/C3 客户端`；Worker 源码 `run_checks.py` 67 个 unittest + mock RPA smoke 通过，包内 `run_checks.py` 67 个 unittest + mock RPA smoke 通过，Win32/OCR window action planning 16 项通过；manifest `forbidden_entries=[]`；已由 v16.11 替代。 |
| v16.11 | `chejin-worker-client-20260630-v16.11.zip` | 历史 C2/C3/C4 测试包 | 基于 v16.10；曾按加好友链路把 C2 定向读取接入通用悬浮球/键鼠守护，并补齐窗口预检 raw/annotated 截图、基线截图、搜索结果候选标注图和 `wechat_messages_targeting_review.html/json`。Windows 实机确认 C2 读消息属于可重试只读动作，不应启用悬浮球；且系统通知可能抢占 foreground 导致窗口预检硬失败，已由 v16.12 替代。 |
| v16.12 | `chejin-worker-client-20260630-v16.12.zip` | 历史 C2/C3/C4 测试包 | 基于 v16.11；修正 C2 定向读取不再启动/报告 OmniAuto 悬浮球键鼠守护，并允许搜索前窗口预检在目标 hwnd 截图/OCR 视觉预检通过时从 foreground 噪声降级继续。但 Windows 实机回归显示失败点后移到清空搜索框阶段：点击搜索框后截图显示微信搜索框已激活，Windows foreground 却返回最小化后台 QQ，导致 `window_guard_failed_before_search_select_all`，已由 v16.13 替代。 |
| v16.13 | `chejin-worker-client-20260630-v16.13.zip` | 历史 C2/C3/C4 测试包，不建议继续测 | 基于 v16.12；曾尝试用 C2 视觉确认降级绕过 Windows foreground 噪声，在清空搜索框和粘贴短码前允许 `foreground_guard_degraded=true` 后继续。该方向经复盘不符合加好友已验证的窗口门禁思路：应优先修 C2 为什么没有把微信真正拉回 foreground，而不是绕过 foreground；已由 v16.14 替代。 |
| v16.14 | `chejin-worker-client-20260630-v16.14.zip` | 历史 C2/C3/C4 测试包，不建议继续测 | 基于 v16.13；撤销 C2 视觉降级绕过 foreground 的逻辑，删除 `allow_visual_foreground_degrade / c2_messages_foreground_guard_can_degrade` 口径；C2 `messages search_by_remark_code` 对齐加好友链路的窗口门禁，在搜索前、清空搜索框前、粘贴短码前均通过 `recover_send_window_guard(hwnd, max_attempts=2)` 主动 `activate_window` 恢复微信 foreground，恢复成功才继续 Ctrl+A/Backspace 或 Ctrl+V，恢复失败则失败并输出截图/OCR/窗口证据；普通 `open_chat`、加好友、C3/C4 发送链路继续保持强窗口守护；新增合同测试复现“点击搜索框后 foreground 一度返回 QQ，但 recover 成功后才允许 Ctrl+A + Backspace”，同时断言 C2 不再出现 `foreground_guard_degraded`；客户端显示版本更新为 `V16.14 · Worker C2/C3 客户端`；Worker 源码 `run_checks.py` 67 个 unittest + mock RPA smoke 通过，包内 `run_checks.py` 67 个 unittest + mock RPA smoke 通过，Win32/OCR window action planning 20 项通过；manifest `forbidden_entries=[]`；Windows 实机回归出现最新 artifact 目录只有 png、没有 `wechat_messages_targeting_review.json/html`，说明 sidecar 中途超时/被杀时未走到 `finish()` 写报告，已由 v16.15 替代。 |
| v16.15 | `chejin-worker-client-20260630-v16.15.zip` | 历史 C2/C3/C4 测试包，不建议继续测 | 基于 v16.14；补齐 C2 `messages search_by_remark_code` 定向读取“失败也必须有报告”的诊断能力：sidecar 每记录一个步骤事件就刷新 `wechat_messages_targeting_review.json/html`，报告 summary 增加 `partial=true/false`，即使后续清空搜索框、粘贴短码、点击候选或窗口守护步骤卡死并被 Worker 超时结束，最新 artifact 目录也应保留最后完成步骤的 partial 报告；最终 `finish()` 成功/失败仍会覆盖为 `partial=false` 正式报告；不改变 C2 窗口门禁，不恢复视觉绕过，不启用悬浮球；客户端显示版本更新为 `V16.15 · Worker C2/C3 客户端`；新增合同测试 `test_search_by_remark_code_writes_partial_report_before_mid_step_crash`，Win32/OCR window action planning 21 项通过；Windows 实机报告显示 `search_box_focus_lost_after_clear`：清空搜索框后微信把焦点落到底部聊天输入框，导致流程未进入粘贴短码/候选点击，已由 v16.16 修复。 |
| v16.16 | `chejin-worker-client-20260630-v16.16.zip` | 历史 C2/C3/C4 测试包，不建议继续测 | 基于 v16.15；修复 C2 定向读取清空搜索框后的焦点丢失问题：`Ctrl+A + Backspace` 清空搜索框后，如果 OCR/视觉确认搜索框已空但焦点落到底部聊天输入框，不再直接失败，而是重新点击侧栏搜索框、复核搜索框激活且查询为空，再继续粘贴 `remark_code`；若重新聚焦失败或搜索框不为空仍然失败，不允许乱输入、不允许读取当前窗口消息；报告中记录 `refocused_after_clear / refocus`；同时补齐 C2 messages 单轮 `sidecar_run_id` 关联链路；针对 Windows 实机报告中空搜索框被 OCR 成 `Q搜索` 的情况，侧车已把 `Q搜索 / O搜索 / 0搜索 / 搜素` 等占位符噪声按空查询处理；同一 `conversation_id + remark_code` 读取失败后进入默认 45 秒短冷却；Worker `run_checks.py` 68 个 unittest + mock RPA smoke 通过，Win32/OCR window action planning 23 项通过；Windows 实机继续暴露两点：一是单次 OCR 慢导致 sidecar 在粘贴前被 Worker 超时杀掉，二是清空搜索框前窗口守护拿到 `left=-32000/top=-32000/160x34` 的最小化/隐藏窗口几何，已由 v16.17 替代。 |
| v16.17 | `chejin-worker-client-20260701-v16.17.zip` | 历史 C2/C3/C4 测试包，不建议继续测 | 基于 v16.16；对照 Windows 人工操作路径修复 C2 非第一屏定向读取前置阶段：`search_by_remark_code` 的 `messages` sidecar 动作预算从默认 visible 读取预算提升到至少 75 秒，进程超时提升到至少 150 秒，避免 Windows 全屏 OCR 9-13 秒/次时还没粘贴短码就被杀；`recover_send_window_guard` 遇到 `left=-32000/top=-32000/160x34` 这类最小化/隐藏几何时，会先恢复/激活目标微信窗口再复检，不再直接以 `window_too_small_for_safe_send` 阻断；C2 清空搜索框函数新增 `search_box_clicked` 和 `search_box_refocused_after_clear` partial step event，点击搜索框后即写入报告，避免中途超时时报告仍停在“基线截图”；客户端显示版本更新为 `V16.17 · Worker C2/C3 客户端`；Worker 源码 `run_checks.py` 68 个 unittest + mock RPA smoke 通过，包内 `run_checks.py` 68 个 unittest + mock RPA smoke 通过；本地 OmniAuto Win32/OCR window action planning 24 项通过；manifest `forbidden_entries=[]`；Windows 实机继续暴露：肉眼可见微信搜索浮层，但报告截图/OCR 未包含浮层，导致候选数为 0，已由 v16.18 改为可见屏幕截图。 |
| v16.18 | `chejin-worker-client-20260701-v16.18.zip` | 当前 C2/C3/C4 测试包 | 基于 v16.17；修复 C2 `messages search_by_remark_code` 搜索结果截图与人眼不一致的问题：搜索结果候选截图从微信主窗口 `PrintWindow` 风格截屏改为与加好友链路一致的 `capture_wechat_window_visible_screen` 可见屏幕截屏，确保微信搜索浮层进入 `wechat_messages_targeting_review.html/json` 和 OCR；当首次可见截图仍未识别到候选时，会执行轻量搜索刷新 `Backspace + SendInput 最后一个字符`，再次等待并用可见屏幕截图生成 `messages_search_by_remark_code_results_after_nudge`，再解析唯一联系人；报告的 `ocr_search_candidates` / `ocr_search_candidates_after_nudge` 记录 `capture_mode=wechat_window_visible_screen`；新增合同测试防止 C2 搜索结果截图退回主窗口截屏；客户端显示版本更新为 `V16.18 · Worker C2/C3 客户端`；Worker 源码 `run_checks.py` 68 个 unittest + mock RPA smoke 通过，包内 `run_checks.py` 68 个 unittest + mock RPA smoke 通过；本地和 fork OmniAuto Win32/OCR window action planning 25 项通过；manifest `forbidden_entries=[]`；Windows 实机重点复测：报告截图应能看到肉眼可见的 `CJWIN01` 搜索浮层，之后进入唯一联系人点击、标题/短码二次确认和消息入库。 |

## 每次发包必须记录

后续每次生成新包，都要在本文档追加一行：

```text
| vN | chejin-worker-client-YYYYMMDD-vN.zip | 当前有效/历史包/废弃 | 本版本做了什么 |
```

同时要记录：

| 项目 | 要求 |
|---|---|
| 打包来源 | 写清基于哪个客户端版本、哪个 OmniAuto 版本或分支 |
| 核心改动 | 写清 UI、RPA、接口、打包依赖分别改了什么 |
| 自测结果 | 至少记录 `run_checks.py` 是否通过 |
| Windows 实机状态 | 写清是否已跑真实微信 RPA |
| 是否可交付测试 | 写清“可测 / 不可测 / 仅 mock 可测” |

## 当前 v16.18 自测记录

| 项目 | 结果 |
|---|---|
| 基线 | v16.17 代码能力；保留 C2 定向读取步骤报告、partial 报告、后端 stale binding 软退役修复、OmniAuto fork-first 同步口径、搜索框强清空保护、窗口截图坐标点击、foreground recovery、`Q搜索` 占位符过滤、失败短冷却、超时预算提升和最小化/隐藏窗口恢复；v15.3 已验收加好友包固定，不再改 v15 系列 |
| Worker 调用入口 | 通过，`OMNIAUTO_ADD_FRIEND_ACTION = "add-friend-entry-click-plan-windows"` |
| Worker 内置 OmniAuto 路由 | 通过，`ADD_FRIEND_ROUTES == ("add-friend-entry-click-plan-windows",)` |
| 旧入口清理 | 通过，包内未包含 `add-friend-entry-click-plan-windows-1080p-reference` 路由；正式 add_friend 入口未回退 |
| Local WeChat UI Lock | 通过，新增本地锁模块与单元测试，支持获取、续租、释放、过期恢复和锁异常错误码 |
| C2 监听循环 | v0.6 口径修订：Worker 启动后独立 C2 listener 在线程中执行第一屏主动扫描、read-targets 状态机定向读取和去重入库；不再执行启动补偿扫描或周期补偿扫描 |
| C2 read-target 字段收口 | 通过，`WechatReadTarget` 包含 `remark_code`；定向读取以 `conversation_id + remark_code` 做身份收口；`row_fingerprint` 仅作 evidence，不再作为阻塞必填 |
| C2 非第一屏定向读取 | 源码合同通过，Windows 待复测；Worker 对非 `visible_hit` 目标调用 `messages --target-mode search_by_remark_code --remark-code <短码>`；侧车必须搜索短码、唯一命中、激活会话并二次确认标题/备注包含短码后才读 messages；v16.18 不为 C2 只读动作启用或报告悬浮球键鼠守护；搜索前窗口预检、清空搜索框前、粘贴短码前如果 foreground 不在目标微信窗口，必须先 `recover_send_window_guard` 拉回微信 foreground，恢复成功才继续，恢复失败不得继续键盘操作；清空搜索框后若焦点落到底部聊天输入框但搜索框为空，会重新点击搜索框并复核激活，再继续粘贴短码；`Q搜索 / O搜索 / 0搜索` 等搜索框占位符 OCR 噪声按空查询处理；遇到 `-32000` 最小化/隐藏窗口几何会先恢复微信窗口再复检；搜索框点击和重新聚焦会即时写入 partial 报告；搜索结果候选截图使用与加好友一致的可见屏幕截图，报告中记录 `capture_mode=wechat_window_visible_screen`；若首次未识别候选，会轻量刷新查询并重新截图；读取失败后同一目标默认 45 秒短冷却，避免连续重试反复清空搜索框；每一轮读取都有唯一 `sidecar_run_id`，目录名、报告 summary、step events、Worker 日志和 ingest evidence 必须一致；失败或超时时查看最新 artifact 目录的 `wechat_messages_targeting_review.html/json`，`partial=true` 表示 sidecar 中途未走到最终 `finish()`，可按最后完成步骤继续定位 |
| C2 payload 映射 | 通过，session scan / message ingest payload 单元测试覆盖短码后追加人工备注识别、文本/图片/voice/低置信 dedupe_key |
| 客户端 `run_checks.py` | 通过，当前源码目录 68 个 unittest + mock RPA smoke 通过；包内 `run_checks.py` 68 个 unittest + mock RPA smoke 通过 |
| OmniAuto add_friend smoke | 通过，35 项 add_friend package smoke checks passed |
| Win32/OCR DPI/窗口尺寸/搜索安全检查 | 通过，window action planning 25 项通过，包含 `CJWCJWIN01WIN01` 拼接查询拒绝合同、`Q搜索` 占位符 OCR 噪声按空查询处理合同、搜索结果候选必须用窗口截图坐标点击合同、搜索浮层必须用可见屏幕截图合同、C2 只读不报告悬浮球合同、搜索前 foreground 恢复合同、最小化/隐藏几何恢复合同、清空搜索框前 foreground 恢复合同、清空后焦点掉到底部输入框时重新聚焦搜索框合同、预检失败截图证据合同、中途异常 partial 报告合同和报告 `sidecar_run_id` 关联合同；v16.18 继承 OmniAuto `#24` 窗口尺寸标准化 |
| Sender role replay | 脚本已打入包内；本机缺 Windows 截图输入时按脚本设计 skipped，需 Windows 实机截图/真实微信回归 |
| Worker UI 基准组件 | 继承 v14，`packages/worker-ui-baseline` fix11 构建产物仍在 `worker-client/chejin_worker_client/web_assets/` |
| 组件化展示层 | 继承 v14，默认入口 `web_ui.py`，QtWebEngine 承载基准组件，QWebChannel 接回 Python 业务层；客户端显示版本为 `V16.18 · Worker C2/C3 客户端` |
| 发包敏感文件扫描 | 通过，源码包构建脚本硬过滤 `.env`、`*.local.env`、runtime、cache、venv、dist、嵌套 zip；manifest `forbidden_entries=[]` |
| zip 结构 | 通过，顶层只有 `worker-client/`；未包含 `frontend/`、`packages/`、`deliverables/`、`.git`、venv、cache、runtime、data、dist、旧 `web-ui-src` |
| C3 Worker 发送链路 | v0.6 口径修订：`chat_reply` 在发送微信前执行 `pre_send_refresh`，发现客户新消息则阻断旧 reply_action 发送；unknown 结果仍上报 `send_result=unknown`，禁止自动补发 |
| C4 Worker 召回前置 | v0.6 口径修订：`follow_up` 不允许直接发送，必须先执行 `recall_precheck_read` 读取微信事实，缺少会话定位或读取失败时禁止发送 |
| 后端 C2 字段 | 通过，`read-targets` 返回 `remark_code / row_fingerprint / ocr_confidence`；后端 C2 回归 19 passed；`scan-result` stale binding 不再物理删除已有消息引用的 binding，改为软退役 |
| zip SHA256 | `af308a43f1b6550e33b14166f282abd890d250bd4e4f90b47699c07d0cf56e9d` |
| zip 大小/文件数 | 3,005,133 bytes；415 files |
| Windows 真实微信 RPA | v15.3 加好友已验收；v16 新增 UI 锁和 C2 监听能力已完成 Windows Worker 实机联调；v16.18 后续实机回归重点为：C2 定向读取期间不应出现 OmniAuto 悬浮球守护；最新 artifact 目录必须存在 `wechat_messages_targeting_review.html/json`；目录名、报告 summary、报告 step events、Worker 日志里的 `sidecar_run_id` 必须一致；点击搜索框后报告应出现 `search_box_clicked`；清空搜索框后若底部聊天输入框获得焦点，应看到 `refocused_after_clear=true` 后继续粘贴短码；空搜索框不应再因为 `Q搜索` 占位符噪声失败；搜索结果截图 `messages_search_by_remark_code_results*.png` 应能包含肉眼可见的微信搜索浮层；同一目标失败后应进入短冷却，不应立即连续多轮清空搜索框；出现 `CJWIN01` 唯一联系人后必须进入候选点击和标题/短码二次确认；继续复测左侧客户文本/语音事实入库、sender_role 左右气泡识别、DPI/窗口定位、后端 scan-result 不再因 binding 外键报 500、加好友主链路、第一屏扫描、C3 pre_send_refresh 和 unknown 不自动重发 |

## Windows 测试命令

```powershell
cd $env:USERPROFILE\Desktop
Expand-Archive -Force .\chejin-worker-client-20260701-v16.18.zip .\chejin-worker-client-20260701-v16.18
cd .\chejin-worker-client-20260701-v16.18\worker-client
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe run_checks.py
$env:CHEJIN_API_BASE_URL="http://192.168.2.100:8000/api"
$env:CHEJIN_RPA_MODE="real"
.\.venv\Scripts\python.exe -m chejin_worker_client.main
```

默认启动的是 V16.18 组件化 Worker 客户端窗口。若 Windows 实机排查时需要临时回退旧 PySide 手写 UI，可额外设置：

```powershell
$env:CHEJIN_WORKER_UI_MODE="pyside"
.\.venv\Scripts\python.exe -m chejin_worker_client.main
```
