# 前端开发完成与自测说明

日期：2026-06-03
提交人：前端负责人
提交给：项目经理
抄送：测试负责人、后端负责人、产品经理、UI 设计师

## 1. 结论

Vite 前端 P0 功能已完成开发、自测和基础联调，申请进入 P0 测试准入评审。

当前前端不是静态展示页，已接入本地后端 API，可执行新增客户、重复手机号提示、线索列表、详情查看、手机号明文查看、导出、批量无效、恢复有效、重新分配、销售轮询开关等 P0 操作。

## 2. 本次完成范围

- 默认空白工作区
- 线索列表
- 线索详情抽屉
- 新增客户
- 重复手机号 `409 / LEAD_PHONE_DUPLICATED` 提示
- 手机号明文查看和审计原因
- 导出选中线索
- 批量标记无效
- 单条标记无效 / 恢复有效
- 重新分配线索
- 销售管理列表
- 销售轮询开关
- 静态稿核心布局迁移
- 静态稿主要文案对齐
- 移动端基础适配，表格在容器内横向滚动

## 3. 运行入口

前端工程：`frontend/`

启动方式：

```bash
cd frontend
npm run dev
```

测试地址：

```text
http://127.0.0.1:5173/
```

后端 API：

```text
http://127.0.0.1:8000/api
```

## 4. 自测结果

| 项目 | 结果 | 说明 |
|---|---|---|
| `npm run build` | 通过 | 最近执行：2026-06-03 08:51 左右 |
| `npm run test` | 通过 | 最近执行：2026-06-03 08:50 左右 |
| Vite 浏览器验收 | 9/9 通过 | 见 `deliverables/test_runs/vite_frontend_smoke_result.json` |
| `npm audit --audit-level=moderate` | 通过 | 最近执行：2026-06-03，0 vulnerabilities |
| 静态稿一致性复核 | 有条件通过 | 核心盒模型、文案和 P0 功能布局已基本对齐 |

## 5. 联调接口

已联通：

- `GET /api/leads`
- `GET /api/leads/stats`
- `POST /api/leads`
- `GET /api/leads/{id}`
- `POST /api/leads/{id}/mark-invalid`
- `POST /api/leads/{id}/restore`
- `POST /api/leads/batch-mark-invalid`
- `POST /api/leads/retry-auto-assign`
- `POST /api/leads/{id}/contacts/{contact_id}/reveal`
- `POST /api/leads/export`
- `GET /api/sales`
- `PUT /api/sales/{id}`

联调说明：

- 当前后端地址使用本地 Docker / FastAPI 服务。
- 当前操作人使用 Header 模拟：`X-Operator-Id`、`X-Operator-Name`、`X-Operator-Role`。
- 重复手机号保存已按后端修复结果返回 `409 / LEAD_PHONE_DUPLICATED`，前端可展示重复提示。

## 6. 验收证据

| 证据 | 文件 |
|---|---|
| Vite 浏览器验收脚本 | `deliverables/test_runs/run_vite_frontend_smoke.js` |
| Vite 浏览器验收结果 | `deliverables/test_runs/vite_frontend_smoke_result.json` |
| UI 截图差异标注 | `deliverables/test_runs/Vite前端UI截图差异标注_2026-06-02.md` |
| Vite 线索页截图 | `deliverables/test_runs/visual_review/vite-leads-desktop.png` |
| 静态稿线索页截图 | `deliverables/test_runs/visual_review/static-leads-desktop.png` |
| Vite 移动端截图 | `deliverables/test_runs/visual_review/vite-leads-mobile.png` |
| Vite 销售页截图 | `deliverables/test_runs/visual_review/vite-sales-desktop.png` |
| P0 静态稿差异必须修改项修复记录 | `deliverables/P0静态稿差异必须修改项修复记录_2026-06-03.md` |

## 7. 已知问题 / 待确认

- 静态稿数据与真实后端数据不同，像素级无法完全一致。
- 默认进入线索页后会选中真实列表第一条数据，展示内容会随后端种子数据变化。
- 详情区已按静态稿补充信息层级，但真实字段与静态稿样例文案仍可能存在差异，需产品/UI 确认可接受。
- `P0静态稿差异确认表_2026-06-03.md` 中 5 个必须修改项已完成修复，需测试复核。
- 完整截图复核脚本 `run_visual_review.js` 最近一次复跑时 Chrome 启动超时，未生成新截图；同一环境下 Vite 浏览器冒烟 9/9 通过。
- 浏览器验收脚本尚未纳入 `frontend` 标准命令或 CI。
- 正式鉴权未接入，当前使用 Header 模拟操作人。
- 销售新增弹窗不在当前 P0 可验收范围内。

## 8. 不包含范围

- 抖音 API 自动接入
- 正式 Excel/CSV 批量导入
- RPA 加微信
- AI 自动回复
- 飞书接管
- 销售登录后台
- 正式鉴权
- 生产环境部署
- 前端 CI/CD 流水线

## 9. 申请结论

前端申请进入 P0 测试准入评审。

建议项目经理基于本说明、后端 Docker 交付状态、接口契约、测试准入清单和最新测试报告，组织测试负责人、后端负责人、产品经理、UI 设计师确认是否允许进入正式 P0 测试。
