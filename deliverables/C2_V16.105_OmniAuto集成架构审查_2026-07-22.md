# C2 V16.105 OmniAuto 集成架构审查

日期：2026-07-22  
审查分支：`codex/c2-v16105-omniauto-integration`  
审查提交：`caf3eb11e21fd6a086b22b1672ba6d88cdbfe84f`  
OmniAuto 来源：`855c21881641cdb2f9fe69d3f2e1caa05e37d04d`

## 1. 审查结论

第 5 步暂不通过，需要退回客户端工程师做一次小范围收口后再复审。

当前没有发现车金 V3、短码 + `private` 准入、角色、语音、去重、停止约束或后端接口被改坏；同时长但距离很远的语音不会被错误绑定，旧图片入口不存在，Vision 未启用。

阻断点不在后端业务合同，而在客户端集成和打包闭环：当前源码、通用 runner、connector、测试和 Windows 打包来源不是同一套版本。

## 2. 必须修复的问题

### P1-1 Windows 打包仍依赖已不存在的旧 ZIP

`worker-client/scripts/build-windows.ps1` 仍固定读取：

`deliverables/omniauto-add-friend-rpa-pr-candidate-20260618.zip`

该文件当前不存在。即使重新放回旧 ZIP，打包脚本也会忽略本分支刚更新的 `worker-client/omniauto-rpa`，可能把旧 OmniAuto 打进 V16.105。

修改要求：

1. Windows 打包默认直接使用当前分支的 `worker-client/omniauto-rpa`。
2. 删除固定旧候选 ZIP、临时解压目录和 `Expand-Archive` 依赖。
3. 保留 sidecar 存在性校验。
4. 更新 `worker-client/tests/test_packaging_scripts.py`，校验打包使用当前分支源码，不再校验旧 ZIP 名称。
5. manifest 增加 OmniAuto 来源提交或关键文件 SHA256，防止以后再次打错版本。

### P1-2 runner、runtime adapter 与 connector 不是同一版本

本次把最新 `wechat_sidecar_runner.py` 和 `wechat_pr28_runtime_adapter.py` 放入车金，但仍保留旧 `wechat_connector.py`。

最新 runner 会传 `conversation_type`，旧 connector 的 `get_messages`、`transcribe_voice_messages`、`send_text` 等方法不接收该参数。当前车金 Worker 直接调用 `wechat_win32_ocr_sidecar.py`，所以 C2 主链暂时不会触发；但内置 OmniAuto 已经形成一套无法正常组合使用的半集成代码。

推荐修改：同步 `855c218` 的 `wechat_connector.py`，但不要接入通用 Scheduler、Vision 或 Brain 编排。若客户端明确决定车金永远不用通用 runner，则应撤回本次 runner/runtime adapter 变更；不能保留不兼容的半套实现。

### P1-3 新安全逻辑没有带齐对应代码和测试

最新测试对车金合并版验证时发现：

1. 输入框缺少可验证边界时禁止点击：已通过。
2. 输入框点击必须落在观测到的内部区域：已通过。
3. C2 同时长、远距离语音禁止误绑定：已通过。
4. 定向搜索前看不到微信搜索框文字证据时应零点击、零按键：车金合并版失败，仍会执行退格键。
5. `humanized_input.apply_interaction_rhythm` 已存在，但 sidecar 缺少同名代理函数。
6. 集成分支保留旧测试，随机节奏测试没有固定随机种子，因此旧测试会产生假失败。

修改要求：

1. 从 `855c218` 定向移植 `sidebar_search_box_evidence` 和 `clear_sidebar_search_box_without_select_all` 的证据式点击逻辑。
2. 增加 sidecar 的 `apply_interaction_rhythm` 代理函数。
3. 同步最新 `run_wechat_win32_ocr_humanized_input_checks.py`。
4. 加入 `run_wechat_win32_ocr_interaction_evidence_checks.py`。
5. 将“唯一但距离很远的同时长语音必须拒绝”用例补入车金实际运行 sidecar 的测试。
6. 不要整份覆盖车金 V3 sidecar，也不要引入 Vision、图片旧入口或通用多会话 Scheduler。

## 3. 已确认通过的内容

- 分支和提交正确，审查前代码工作区干净，`git diff --check` 通过。
- 5 个改动文件静态编译通过。
- 车金 `RpaBridge`、C2 语音、会话准入、旧 sidecar 隔离和打包静态规则：`103 passed`，另有 `8 subtests passed`。
- 远距离同时长语音验证：`accepted=false`、`selected_candidate_count=0`。
- 最新 PR #28 runtime adapter 测试对车金合并版：5/5 通过。
- 旧图片保存入口 `wechat_image_save_capture.py` 不存在。
- 未新增后端接口、消息字段、角色规则或去重规则。
- 未启用 Vision，未修改后端，未升级版本，未生成安装包。

## 4. 最短修复顺序

1. 客户端工程师先修打包来源，确保 V16.105 一定取当前分支 `omniauto-rpa`。
2. 补齐 connector 与 runner/runtime adapter 的版本一致性。
3. 补搜索框证据逻辑、humanized facade 和三组定向测试。
4. 只跑离线定向回归并提交一个收口 commit，不升级版本、不打包。
5. 架构师复审该 commit；通过后再进入后续串行步骤。

## 5. 回滚

本轮尚未生成 V16.105，最简单回滚仍是 V16.104：

- 车金旧基线：`259976e24e5c83eefc720b43ad2910865051c875`
- OmniAuto 旧基线：`8f832dd7e2ed78ff5535924b12818475de066a27`
- 已通过安装包：`deliverables/chejin-worker-client-20260721-v16.104.zip`

在第 5 步复审通过前，不修改 V16.104 安装包，不进入 Windows 完整实机测试。

## 6. `c46bfc8` 复审结果

复审提交：`c46bfc88bd22a181bec33878444435defad4f705`

### 已闭合

1. Windows 打包已改为直接使用当前分支 `worker-client/omniauto-rpa`，旧 ZIP 和解压逻辑已删除。
2. 打包后会比较源码 sidecar 与产物 sidecar 的 SHA256；manifest 记录 OmniAuto 来源提交及两份哈希。
3. connector 已与 `855c218` 对齐，仅保留车金所需的 `conversation_type` 请求解析补充。
4. runner、runtime adapter、connector、sidecar 的 `conversation_type` 参数合同已统一。
5. `conversation_type` 没有参与 sidecar 放行；C2 仍由标题 OCR 证明“有效短码 + private”。
6. 搜索框无 OCR 证据时鼠标零点击、键盘零按键。
7. 输入节奏代理、远距离同时长语音拒绝、旧图片入口删除和 Vision 禁用均符合预期。

### 验证通过

- Worker 全量 `unittest discover`：共运行 241 项，`212 passed`、`29 skipped`；跳过项为已删除的 V1/V2 消息组装测试。
- 交互证据：5/5 通过。
- 输入节奏：6/6 通过。
- 远距离同时长语音拒绝通过。
- Python 编译、`git diff --check`、工作区状态通过。

### 剩余阻断：测试合同未完全同步

第 5 步仍暂不通过，但不需要再修改 C2 运行逻辑，只需客户端工程师收口测试。

1. 完整 `run_wechat_win32_ocr_compat_checks.py` 在依赖补齐后实际仍失败。首个失败用例 `test_input_fast_visual_confirm_keeps_before_ocr_and_skips_after_ocr` 的假输入区域仍是旧结构，缺少新安全逻辑要求的 `reason` 和 `bounds`。
2. `run_wechat_win32_ocr_window_action_planning_checks.py` 的 `test_search_result_candidate_uses_window_image_click_coordinates` 使用旧假验证结果，缺少 `conversation_type=private` 和 `conversation_type_evidence.short_code_confirmed=true`，因此被车金正确的私聊准入门禁拒绝。
3. `build-windows.ps1` 只调用 `run_checks.py`；当前 `run_checks.py` 没有执行新增的 interaction evidence、humanized input 和 window action planning 三个专项脚本，新测试尚未成为正式打包门禁。

### 客户端工程师最短动作

1. 只更新上述两个旧测试夹具，不降低生产代码的证据和私聊准入要求。
2. 将 interaction evidence、humanized input、window action planning 三个专项脚本纳入 `worker-client/run_checks.py`。
3. 使用锁定依赖完整跑通 `run_checks.py` 和三个专项脚本，不能只跑挑选用例。
4. 提交一个纯测试/门禁收口 commit 后交架构师终审；仍不升级版本、不打包。
