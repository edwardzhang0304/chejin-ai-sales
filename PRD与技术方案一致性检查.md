# PRD 与技术方案一致性检查

版本：v0.8.9

日期：2026-08-10

## 1. 检查对象

- PRD：`deliverables/AI智能客服售前跟进系统_PRD_运营后台统一版_v0.5.3.md`
- 技术方案：`deliverables/AI智能客服售前跟进系统_技术方案手册_v0.8.6.md`
- 接口合同：`deliverables/C2-C3_OmniAuto_Worker_后端接口合同_v0.1.1_2026-08-05.md`
- C2 实机报告：`deliverables/C2_Windows实机验收报告_2026-08-03.md`
- 车金本轮快速 UAT 实际基线：`9872dad3c469b4d9c1cc328060f04fb1e0c3e139 / v16.145.0`；此前整改父基线：`3f65660fb712a14527ea1307715a8c2dacb9c8b1`
- 车金当前未发布运行整改提交：`53e979d89316344c282245c62f6349586de16eee`
- 独立 OmniAuto 当前候选：`91688de9047d5973cee9b18de00ca2f6e7772a86`；内嵌基础仍为包含 `35b0eee` 的 `a563e668…`
- 正式回滚证据：`v16.130.0 / 8ee53e1`

## 2. 结论

PRD v0.5.3 已同步三层职责、C2 private/V3 准入、文字/语音/图片统一事实、
事实结算、C2-C3 单会话串行、C3 回复、任务中心发送和停止语义；现有主链路未发现
需要重开产品决策的 P0 冲突。运营后台登录的产品与技术口径已经冻结：指定账号密码
登录、服务端可撤销 Cookie 会话、登录即全部权限、不做 RBAC，并与 Worker Token
完全隔离。当前前后端仍是旧 Bearer/角色门禁实现，属于待开发差异，不再是待决策项。

双仓候选曾在 `a563e668…` 基础上选择性接入 OmniAuto `91688de…`，但随后
`v16.145.0 / 9872dad…` 快速 UAT 又确认三项 P0：同一物理语音产生多个正式身份、身份
异常可早于媒体逐条终态结算返回、首屏命中绕过成功冷却。技术方案已经固定一个物理语音
一个正式对象、多 anchor 只作 aliases、冻结集合逐条终态和三入口统一冷却；隔离候选及
生产链自动测试已经完成，架构复审通过前仍不得生成替代 ZIP、正式 EXE 或进入下一轮
Windows UAT。

同时，自动回复口径已从“遇到任一异常即长期 handoff”改为 L0—L3 分级。安全边界之前的
旧问题不阻断，身份/历史/技术暂态先自动恢复；当前客户单条语音/图片失败完成事实结算后
直接转人工且不生成重发提示，高意向通知销售并转人工；明确拒收、目标不明、发送未知、
人工接管及必须人工决定的硬风险同样停止普通 AI。该修订不放宽目标、发送和 Guard 安全边界。

## 3. 一致性矩阵

| 检查项 | 产品/技术口径 | 当前实现与证据 | 判断 |
|---|---|---|---|
| C2 会话准入 | OmniAuto 是标题、会话类型、固定八位短码和准入的唯一判定者；Worker 只校验合同与后端授权 | 已删除 Worker 对 `raw_title` 的解析和会话重分类；缺失、矛盾或非 `private + allowed=true + 唯一正式短码` 全部失败关闭并告警 | 已整改，待架构复审 |
| 首屏与定向 | 首屏可发现事实；未授权不读取，授权后才定向 | 多目标自动发现和串行实机通过 | 一致 |
| 文字/语音/图片 | 统一角色、身份、顺序和 V3 ingest | `v16.130.0` 图片、顺序、去重实机通过 | 一致 |
| 图片失败 | 结构化失败，不伪装零图片；菜单失败只用四个正式 `reason_detail`；PID 不是硬门禁；真实生产端必须无条件形成终态 | `not_attempted` 菜单失败已由真实 `process_image_slot` 生产路径写 terminal | 已实现，待架构复审 |
| 语音/图片类型仲裁 | 先保护可靠文字/语音结构，只对未解释区域生成图片候选；任一可靠类型证据即可否决冲突图片候选 | 已覆盖连续展开语音、动作失败/无动作证据及图片内时长 OCR 反向边界 | 已实现，待架构复审 |
| 语音唯一身份与逐条结算 | 一个物理语音一个 `canonical_voice_id`，多 anchor 只作 aliases；冻结候选全部 completed/failed 后才允许异常退出 | `9872dad…` 现场链已在隔离候选补生产测试 | 已实现，待架构复审 |
| AI 回复门禁 | 只保护最新 `reply_safe_suffix`；L0 继续、L2 自动恢复；当前客户媒体失败走 L1 handoff，高意向及其他硬风险走 L3 | customer/self 媒体分级、高意向直接 handoff 及合同 `3.12.7` 已实现 | 已实现，待架构复审 |
| 读取冷却 | 首屏、定向和恢复入口共用成功/失败/后端 due；完成后的新未读可唤醒 | `9872dad…` 现场链已在隔离候选补生产测试 | 已实现，待架构复审 |
| C3 自动回复 | Brain/Guard → reply_action → 当前 C2 flow 发送 | 此前实机测试且当前无已知问题 | 一致 |
| 召回任务类型 | recall_precheck → trigger_type=recall 批次 → chat_reply；无独立 follow_up | PRD 已删除 follow_up 当前任务口径 | 一致 |
| 停止监听 | 可保留首屏事实扫描；不得定向、读取、识图、入库或发送 | C2 实机通过 | 一致 |
| 事务恢复 | UI 授权、事实结算、状态推进分离；`not_attempted` 不能覆盖既成事实 | 已用真实生产入口证明 A 失败结算后释放全局门禁并继续 B；旧空 Journal + 已确认 Ledger 可自动收尾 | 已实现，待架构复审 |
| 双仓来源 | OmniAuto 通用能力只有一个上游，车金只保留适配 | 基础 `a563e668…`，活动选择性来源 `91688de…`，机器 scope 已登记可靠类型优先仲裁，绝对 Vision 边界门禁 9/9 通过，车金整改父基线 `3f65660…` | 来源一致，待架构复审 |
| Worker 客户端进度 | 本地 UI 只展示 Worker 已知阶段；OmniAuto 返回后再补充详细结果 | 不改服务端字段和任务状态，不将 RPA 内部步骤伪装成实时进度 | 一致 |
| 运营后台登录 | 服务端预建账号；账号密码登录；可撤销 Cookie 会话；登录即全部权限；与 Worker Token 隔离 | 技术方案 v0.8.6 已固定；当前实现状态需按对应专项证据判断 | 产品/技术一致 |
| 第一期车辆管理 | 只做列表、详情/编辑、图片、上下架和 Excel 三步导入；不做独立知识规则页 | PRD v0.5.3 与技术方案 v0.8.6 第 9.3 节一致 | 产品/技术一致 |
| 车辆对客字段 | 只有已上架车辆的客户可用字段可进入回复；VIN、车牌号、采购价格和内部备注禁止对客 | PRD v0.5.3 与技术方案 Product Master customer-safe projection 一致 | 一致 |

C4 配置已经统一：连续 72 小时无新消息后检查，单客户最多 3 个周期、每日最多 1 次、
21:00—次日 09:00 静默，文案由 Brain/Guard 生成。技术方案中的“每客户最多一次”
只存在于旧口径列，不是当前规则；C4 不再有待开发或待决策的次数冲突。

## 4. 当前风险与处理

| 风险 | 小白解释 | 固定处理 |
|---|---|---|
| 再次在车金内嵌副本独立修改通用 OmniAuto | 会重新制造两份源码 | 通用改动仍先上游，车金只更新固定提交与适配 |
| 整目录覆盖 | 会把两边独有修复一起冲掉，表面能运行但历史能力可能退化 | 逐文件审查和移植，禁止复制整个目录 |
| 把 UAT 结论套给新包 | `v16.130.0` 通过不代表后续源码统一包自动通过 | 新版本重新跑自动化；运行代码变化时做受影响范围 Windows 冒烟 |
| 历史文档/ZIP 干扰 | 工程师可能从旧包或旧说明继续修改 | 活跃目录只留当前方案和验收元数据，旧物已移出 |
| 用 skip 掩盖失败 | 测试看起来绿色，实际功能根本没执行 | 删除只针对已移除 V1/V2 流程的永久跳过测试；新增 skip 必须评审 |
| 为提高回复率而放松硬安全边界 | 可能回复错人、重复发送或编造事实 | 只放宽可恢复技术门禁；目标不明、发送未知、明确拒收和 Guard 不通过仍保持硬停止 |

## 5. 后续车辆信息提交门禁

1. OmniAuto 通用改动只包含通用能力和通用测试，不含车金业务字段或后端流程。
2. 车金固定引用已审核的 OmniAuto 提交，不引用浮动 `master`。
3. 来源元数据、目录 SHA、合同 revision、生成 schema 和 manifest 与代码一致。
4. Worker 全量检查、后端 C2/C3 回归和 `git diff --check` 全通过，且没有新增永久
   skip。
5. 新版本号高于 `16.130.0`；包、提交、manifest、SHA 一一对应。
6. 任何回归无法在当前分支确定修复时，回滚到 `8ee53e1 / v16.130.0`，不从历史
   ZIP 继续开发。
7. `20260809_0025` 升级前备份数据库；应用允许回滚但新增审计表保留，不执行普通
   Alembic downgrade 跨过该迁移。
